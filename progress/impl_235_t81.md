# 235 · T8.1 — los dos defectos de la card de «Con ayuda solicitada»

Encontrados conduciendo la app con Playwright, no por la suite. Los dos viven en
`renderCardConAyuda` (`app/(app)/mis-asignaciones/_components/RepartoModule.tsx`) y son una prop
cada uno. Nada más se tocó.

---

## Defecto 1 — el chip decía «En reparto»

`PosOrderCardDetalle` resuelve `const estado = estadoProp ?? estadoPorDefecto(esActiva, esDetalle)`,
y la card montaba los dos flags en `false` sin `estado`: `estadoPorDefecto(false, false)` devuelve
el literal `"En reparto"`. Para los otros tres valores («En gestión», «En detalle», «Por recoger»)
el chip describe la situación de la orden; aquí afirmaba **lo único que esta ficha volvió falso**, a
un palmo de un encabezado que dice lo contrario.

**Arreglo:** `estado={AYUDA_CARD_ESTADO}`, constante nueva junto a los otros textos de la sección
(i18n-ready, como el resto del archivo).

### El rótulo: «En ayuda»

Contrastado contra los tres vecinos que ya existen, antes de elegirlo:

| Fuente | Texto | Qué aportó a la decisión |
| --- | --- | --- |
| `pos-estado.ts` · `PosEstado` | «En gestión» / «En detalle» / «En reparto» / «Por recoger» | La **gramática** de la familia: preposición + sustantivo, dos palabras. «En ayuda» entra sin ser un cuerpo extraño. |
| `RepartoModule` · `AYUDA_SECCION_TITULO` | «Con ayuda solicitada» | No se repite palabra por palabra, pero **comparte «ayuda»**: no es un tercer sinónimo. |
| `EstatusBadge` · `ORDER_STATUS_LABEL.ayuda_tienda` (R37) | «Ayuda solicitada a la tienda» | Ídem, y su propio comentario dice que la forma corta se descartó **por ambigua en `/ordenes`**, donde maestro/admin la ven suelta entre veintiún estados y no sabrían a quién se le pidió. |

Ese motivo **no aplica aquí y por eso la forma corta sí sirve**: el chip vive DENTRO de la sección
«Con ayuda solicitada», cuyo texto de ayuda ya dice a quién («Tu tienda las está viendo en
Novedades»). La desambiguación está pegada, no a veintiún estados de distancia. Y el chip es una
fila apretada junto al nº de remisión y al nombre: 9 caracteres frente a 28.

### El color: el fallback es correcto, y se dejó dicho

Con texto libre `estadoBadgeClass` cae a las clases de «En reparto» = `bg-warning text-navy` (lo
dice su propio comentario, y el rojo de la mutación 1 lo confirmó en el DOM real). Comprobado que
**`warning` es la familia correcta**, no una casualidad:

- `EstatusBadge` ya asignó `ayuda_tienda: "warning"` (R37) con su razón escrita al lado: es la
  variante que este repo da a los **estados de espera con acción pendiente** (`por_devolver`,
  `sin_gestionar`, `devuelta`, `devolucion_por_confirmar`). Ni `danger` ni `info`.
- El encabezado de esta misma sección ya usa `text-warning-strong`.
- `DESIGN.md` (feature 208): `warning` y `navy` son **tokens fijos** del bloque `@theme`, así que
  `bg-warning text-navy` es fijo-sobre-fijo — el emparejamiento correcto —, medido 8.1:1 en los dos
  temas según el comentario de `ESTADO_CLASSNAME`.

~~**Por eso NO se añadió entrada propia a `ESTADO_CLASSNAME`:** sería `"En ayuda": "bg-warning
text-navy"`, un duplicado literal del fallback al que ya llega. Se documentó en el comentario del
call site para que nadie lo lea como un accidente.~~

> **Revertido el 2026-08-19 por N2 de `progress/review_235_r2.md`: la entrada SÍ se añadió.**
> El porqué, la mutación y el rojo, en «N1 y N2 — el acabado de la re-revisión» al final de este
> archivo.

---

## Defecto 2 — la card llevaba las marcas de ruta (R15)

> **R15.** … NO DEBE pintarla como parada en el mapa de ruta del mensajero **ni contarla entre las
> paradas pendientes de optimizar**.

El servicio ya cumplía el número (`MisAsignacionesService` las deja fuera de `paradasSinOptimizar` y
del mapa, con test), pero la card seguía luciendo el nº de parada («·», con su «Sin posición en la
ruta» accesible) y el badge «Pendiente de optimizar». La lógica migró, la superficie no.

**Arreglo:** `mostrarRuta={false}`, la prop que el repo ya tenía para esto («`false` para
superficies sin ruta optimizada ("Por recoger")»). Esta sección es exactamente eso: sus órdenes
llegan **sin `secuenciaRuta`** a propósito.

### Qué se comprobó ANTES de usarla

Leído `PosOrderCardDetalle.tsx` (líneas ~90-115) en vez de fiarse de la descripción. `mostrarRuta`
aparece en **dos sitios y solo dos**:

1. `{mostrarRuta ? …}` → el `<span>` del nº de parada, que contiene el `sr-only` «Sin posición en la
   ruta» / «Parada N de TOTAL».
2. `{mostrarRuta && orden.secuenciaRuta === null ? <Badge>Pendiente de optimizar</Badge> : null}`.

**No se lleva nada por delante:** `UbicacionTrigger` (navegar), el nº de remisión, el chip de estado,
el badge «Gestionar más tarde» (`orden.marcarLuego`, condición independiente), el destinatario, la
línea de ubicación, los intentos, el monto a cobrar y el slot `acciones` (Recuperar + Conversación)
quedan intactos. No hizo falta alternativa.

---

## Tests — `tests/components/RepartoAyuda.test.tsx`

Dos casos nuevos, emparejados en estilo con los R15/R16/R19/R35 que ya viven ahí:

1. `235/R37: el chip de la card NO dice «En reparto», que es lo que la orden dejó de ser`
   (en el `describe` de la sección, junto a los otros casos de card).
2. `235/R15: su card NO lleva la marca «Pendiente de optimizar», que sí sigue en el listado de arriba`
   (en el `describe` de R15).

El segundo lleva **las dos mitades a propósito**: sin la orden del listado principal con
`secuenciaRuta: null`, el caso quedaría verde también si la marca se apagara en TODAS partes o no se
pintara nunca. Se le sumó que tampoco se anuncie «Sin posición en la ruta»: sin ruta no hay posición
ausente que declarar.

## Mutaciones — cada rojo VISTO, con su mensaje real

sha256 de `app/(app)/mis-asignaciones/_components/RepartoModule.tsx`:

| Momento | sha256 | Resultado de `pnpm exec vitest run tests/components/RepartoAyuda.test.tsx` |
| --- | --- | --- |
| Antes (baseline) | `c0bfdf95e19feb89142312226fc0228b7c07aa80e9bed6eeb944ee168490597f` | — |
| Después (arreglado) | `1cd40aba95d5155f60c73815e65a1e72f9f5e375eee81eae21eedf4812d2c9a5` | **17 passed** |
| M1 · sin `estado={AYUDA_CARD_ESTADO}` | `56c79e2c7c131c69fbc0c6e4e11abfc5a9e9fb8e3b07cc0009a41c8fd26e48c0` | **1 failed / 16 passed** |
| M2 · sin `mostrarRuta={false}` | `6fbb4d0172300524cdf2ac309c135a314ba05cfd947033dc1245f1b5e2619598` | **1 failed / 16 passed** |
| M3 · `mostrarRuta={false}` TAMBIÉN en `renderCardEnReparto` | `dacda3d9635e22c7befb569ff8d757d5981ca33c8bd41805b76763c85a84fa86` | **1 failed / 16 passed** |
| Restaurado | `1cd40aba95d5155f60c73815e65a1e72f9f5e375eee81eae21eedf4812d2c9a5` | **17 passed** |

sha256 de `tests/components/RepartoAyuda.test.tsx`: antes
`97fd27f9080580829ecf0925907863f95a725c153de1dc4f08ee4f8599623055`, después
`3ba02ba8a787b2eadfb79790ee3403c1521caba2db3c3a8cf6e13c32ebb307f5` (sin tocar durante las
mutaciones).

**M1 — quitada la prop `estado`.** Cae el caso R37, y de paso el rojo IMPRIME el fallback de color,
que es la prueba de que `bg-warning text-navy` es lo que se hereda:

```
FAIL  tests/components/RepartoAyuda.test.tsx > Reparto · las órdenes con ayuda se van abajo, a su propia sección > 235/R37: el chip de la card NO dice «En reparto», que es lo que la orden dejó de ser
AssertionError: expected <span …(1)></span> to be null

- Expected:
null

+ Received:
<span
  class="rounded-full px-1.5 py-0.5 text-[9px] font-bold tracking-wide bg-warning text-navy"
>
  En reparto
</span>
 ❯ tests/components/RepartoAyuda.test.tsx:367:55
```

**M2 — quitada la prop `mostrarRuta`.** Cae el caso R15:

```
FAIL  tests/components/RepartoAyuda.test.tsx > Reparto · la orden en ayuda sale de la ruta, pero no del chat > 235/R15: su card NO lleva la marca «Pendiente de optimizar», que sí sigue en el listado de arriba
AssertionError: expected <span data-slot="badge" …(2)></span> to be null

- Expected:
null

+ Received:
<span … data-variant="outline">
  Pendiente de optimizar
</span>
 ❯ tests/components/RepartoAyuda.test.tsx:428:67
```

**M3 — la mutación de control: apagar la ruta EN TODAS PARTES.** No la pedía el encargo; se corrió
para demostrar que la segunda mitad del caso no es decorativa. Cae por la aserción del listado
principal, la contraria a la de M2:

```
FAIL  … > 235/R15: su card NO lleva la marca «Pendiente de optimizar», que sí sigue en el listado de arriba
TestingLibraryElementError: Unable to find an element with the text: Pendiente de optimizar.
 ❯ tests/components/RepartoAyuda.test.tsx:433:28
```

## Verificación

```
$ pnpm exec tsc --noEmit
EXIT=0                                    (sin salida)

$ pnpm exec eslint "app/(app)/mis-asignaciones/_components/RepartoModule.tsx" "tests/components/RepartoAyuda.test.tsx"
EXIT=0                                    (sin salida)

$ pnpm exec vitest run tests/components/RepartoAyuda.test.tsx tests/components/RepartoModule.test.tsx \
    tests/components/MarcarLuegoToggle.test.tsx tests/components/GestionarOrdenPanelHilo.test.tsx \
    tests/components/ChatConversacionTono.test.tsx tests/unit/guards/superficie-de-uso.guardia.test.ts \
    tests/unit/guards/entregas-sin-recoleccion.test.ts
 Test Files  7 passed (7)
      Tests  152 passed (152)

$ pnpm exec vitest run tests/unit/guards
 Test Files  57 passed (57)
      Tests  834 passed (834)
```

Sin commit. `RecuperarAyudaButton.tsx` y el caso `235/R25` no se tocaron.

---

# N1 y N2 — el acabado de la re-revisión

Los dos únicos menores de código que dejó `progress/review_235_r2.md` (§4 y «Menores nuevos»), con
el veredicto ya en **OK**. Solo frontend: dos archivos de `app/` y un caso de test. No se tocaron
`RecuperarAyudaButton.tsx`, el caso `235/R25` ni los dos casos de T8.1 (`235/R37`, `235/R15`).

## N1 — el comentario que justificaba con un hecho falso

**Comprobado en el fuente antes de decidir nada, no leído del acta:**

- `renderCardConAyuda` no pasa `onGestionar` (`RepartoModule.tsx`).
- `posSeleccionHandlers` (`pos-card/pos-seleccion.ts`): `const seleccionable = Boolean(onGestionar) && !bloqueado`.
  Sin `onGestionar` es `false` con cualquier `bloqueado`.
- `PosOrderCardDetalle.tsx`: `bloqueado` aparece **una sola vez**, en la llamada a
  `posSeleccionHandlers`; de ahí salen `seleccionable`/`onClick`/`onKeyDown`, que gobiernan
  `tabIndex`, el `cursor-pointer` y el foco. El slot `acciones` se pinta tal cual, y el otro botón
  del slot («Conversación») tampoco está gateado.

Confirmado: **nada de esta card está bloqueado por `bloqueado`** y la prop es **inerte**.

### Opción elegida: (a) — corregir la frase y CONSERVAR la prop

Coincide con la inclinación del encargo, y este es el razonamiento propio:

1. La prop es inerte **por una coincidencia** (que esta card no monte acción de selección), no por
   una decisión de que el bloqueo no aplique aquí. Un `bloqueado` retirado y un `bloqueado` que hoy
   no hace nada **se leen igual dentro de un año**; lo que distingue uno de otro es la razón escrita.
2. El futuro no es especulativo: **la 237 está nombrada en este mismo archivo** como la ficha que
   devuelve las gestiones desde ayuda. Si vuelve una acción de selección con la prop quitada, el
   bloqueo del mensajero (111/R14) dejaría de aplicar **en silencio** — nadie tiene por qué acordarse
   de reponer una prop que no está.
3. Quitarla no arreglaba el defecto real, que era **la frase**, no el JSX: el comentario seguiría
   siendo el único sitio donde alguien puede enterarse de por qué el gate está apagado.

Contra (b) no hay «código muerto» que borrar: `bloqueado` tiene default `false` en el componente, así
que quitarla o dejarla es **idéntico en conducta hoy**. Lo único que cambia es cuánta información
queda escrita.

### Qué se escribió

| Sitio | Cambio |
| --- | --- |
| `RepartoModule.tsx`, prop `bloqueado={bloqueado}` de `renderCardConAyuda` | Comentario nuevo: por qué **hoy no apaga nada** (el cálculo de `posSeleccionHandlers` y la ausencia de `onGestionar`) y por qué se conserva igual (la 237). |
| `RepartoModule.tsx`, comentario de `RecuperarAyudaButton` (R25) | Se cae la premisa falsa. Queda: «no hace falta apoyarla en que *el resto de la card sí está bloqueada*, que además hoy es falso — se sostiene sola», y sigue **intacto el argumento bueno**: R25 + el deadlock de `lib/services/rescate-ayuda.ts` + R22 + el permiso inejercitable de R35. |
| `RepartoModule.tsx`, JSDoc de `renderCardConAyuda` | «— es la única excepción de esta card» → fuera. Implicaba lo mismo que la premisa falsa, y además no era exacto ni en la pantalla (el `MarcarLuegoToggle` de la card normal también sobrevive al bloqueo, por su propia razón escrita). |
| `progress/impl_235_m4.md` §1 | La misma premisa, tachada en el sitio + nota fechada con la corrección. No se reescribió el resto de la bitácora. |

**Había una TERCERA copia, y se cerró también.** El comentario del caso `235/R25` en
`tests/components/RepartoAyuda.test.tsx` (l. 338) repetía la misma premisa falsa: *«El resto de la
card SÍ sigue bloqueada; esta acción es la salida»*. Quedó anotada como intocable porque el encargo
original protegía ese caso; el coordinador **levantó la prohibición para esto y solo esto**, así que
se corrigió. Es un cambio de UNA línea de comentario a tres — diff comprobado: ni una aserción, ni
el nombre del caso, ni `renderModule`. Texto nuevo:

> «… el permiso inejercitable que R35 prohíbe. **No se apoya en que el resto de la card siga
> bloqueada: no lo está** —sin `onGestionar` el gate de selección ya está apagado, ver el comentario
> de la prop `bloqueado` en `RepartoModule`—. El `alert` de abajo es lo que prueba que el bloqueo
> llegó al módulo.»

El argumento bueno (R22 + el deadlock de `rescate-ayuda.ts` + R35) ya estaba en ese comentario y **no
se repitió**. Verde después de tocarlo: `Test Files 1 passed (1)` / `Tests 18 passed (18)`; sha256 del
archivo `2d0841ed…` -> `b41122f50e389d8850585db745dbefe6688838b6e865295798126ce5bdbd58a1`, y `eslint`
sobre él, EXIT=0. **Ya no queda ninguna copia de la premisa falsa en el repo.**

## N2 — el color del chip deja de viajar en el fallback

### La entrada

`app/(app)/mis-asignaciones/_components/pos-card/pos-estado.ts`:

```ts
"En ayuda": "bg-warning text-navy",
```

Con su comentario, que dice **por qué `warning`** (familia de los estados de **espera con acción
pendiente** — la misma que `EstatusBadge` asigna a `ayuda_tienda` y la del `text-warning-strong` del
encabezado de la sección; ni `danger`, que no hay fallo, ni `info`), **por qué sólido y no suave**
(`bg-warning text-navy` es fijo-sobre-fijo, la regla de `DESIGN.md` para chips sólidos y los 8.1:1 ya
medidos arriba en ese mismo archivo — el `Badge variant="warning"` es `bg-warning-soft
text-warning-strong`, misma familia y otro tratamiento) y **por qué se declara aunque coincida con el
fallback** (el fallback significa «no sé qué es este rótulo»: heredar de él una decisión de color la
vuelve indistinguible de un accidente).

### El tipo `PosEstado`: NO se toca, y por qué

Se miró, que era la pregunta. **No obliga a nada:** `ESTADO_CLASSNAME` es `Record<string, string>`,
`estadoBadgeClass` recibe `string` a propósito y la prop de la card es `estado?: string`. `PosEstado`
solo aparece como **tipo de retorno de `estadoPorDefecto`**, y no se importa en ningún otro archivo
del repo.

Añadir «En ayuda» a la unión habría hecho que `estadoPorDefecto` **anunciara un valor que nunca puede
devolver** (deriva de `esActiva`/`esDetalle` y nada más; «En ayuda» la pasa el consumidor). Se hizo
lo coherente con cómo el archivo declara las cosas: **precisar su JSDoc** —que decía «etiqueta que
las cards saben pintar», ya falso con cinco entradas en la tabla— para que diga lo que la unión ES
(lo que `estadoPorDefecto` deriva) y por qué `estadoBadgeClass` recibe `string`.

### El test

`tests/components/RepartoAyuda.test.tsx`, caso nuevo junto al de R37 (que **no se tocó**):

> `235/R37: y el COLOR de ese chip es el de warning sólido, fijado y no heredado`

Mide la **superficie**, no la tabla: renderiza el módulo y afirma que el chip «En ayuda» de la sección
lleva `bg-warning` y `text-navy`. Da igual por qué rama del código llegue el color: si cambia, el caso
cae.

### La mutación que prueba algo — y la que no

**La que NO prueba nada, y se corrió para demostrarlo:** *borrar la entrada*. El rótulo vuelve a caer
al fallback, que da **el mismo color**, así que el test sigue verde — correctamente: lo que se fija es
el color del chip, no la existencia de una línea. Por eso hacían falta mutaciones que **cambien el
color**.

| # | Mutación | sha256 de `pos-card/pos-estado.ts` | `pnpm exec vitest run tests/components/RepartoAyuda.test.tsx` |
| --- | --- | --- | --- |
| — | antes (baseline de la re-revisión) | `d26c16523c8599a9ef08afcfbbd7dea3d16e48d1fbe8104e7317e63423ab247a` | — |
| — | después (arreglado) | `655ac487a93a68fee08626e305f3b3802aec0b88285f1fb898515d3c356db2c7` | **18 passed (18)** |
| **MC** | control: borrada la entrada, fallback intacto | `4056470e63b94dbef61d32633cfe891fd6165e25715a7795e5606a656c7f3d40` | **18 passed (18)** — SOBREVIVE, como se predijo |
| **MA** | la entrada, con otro color: `"En ayuda": "bg-info text-white"` | `168e26faeef378e57a87380c75dc78333ff4c2ab1bd6a8828f1142205ee6e78f` | **1 failed \| 17 passed** |
| **MB** | el defecto REAL: sin entrada **y** alguien retoca `"En reparto"` a `bg-brand-light text-navy-deep` | `59a75e6e635b8fb2a1d773e30b4b76fa3bdcf23a196a300e79760bb9d4a916b8` | **1 failed \| 17 passed** |
| — | restaurado | `655ac487a93a68fee08626e305f3b3802aec0b88285f1fb898515d3c356db2c7` | **18 passed (18)** |

**MB es la mutación que importa**, porque es exactamente el riesgo que el acta describe: hoy el chip
tiene entrada propia, así que retocar «En reparto» **ya no lo mueve** — y si alguien deshiciera las
dos cosas a la vez, el caso lo dice. Los dos rojos, leídos de la salida real:

```
FAIL  tests/components/RepartoAyuda.test.tsx > Reparto · las órdenes con ayuda se van abajo, a su
propia sección > 235/R37: y el COLOR de ese chip es el de `warning` sólido, fijado y no heredado
Error: expect(element).toHaveClass("bg-warning text-navy")

Expected the element to have class:
  bg-warning text-navy
Received:
  rounded-full px-1.5 py-0.5 text-[9px] font-bold tracking-wide bg-info text-white               <- MA
  rounded-full px-1.5 py-0.5 text-[9px] font-bold tracking-wide bg-brand-light text-navy-deep    <- MB
 ❯ tests/components/RepartoAyuda.test.tsx:383:51
```

(Dos corridas distintas; se pegan juntas la línea `Received` de cada una porque el resto del mensaje
es idéntico carácter a carácter. En las dos el ÚNICO caso rojo fue el nuevo.)

Los otros dos archivos tocados no se mutaron y no cambiaron durante todo esto:
`RepartoModule.tsx` = `854a04eca2fceaa0f1f1275b3a004bb0335c9189766d0284206195cf8a16e8d7`,
`tests/components/RepartoAyuda.test.tsx` = `2d0841eddd4a2b6e7647deb70c1077a30244b889bc03d8b14099edd05190cac1`
(y `b41122f50e389d8850585db745dbefe6688838b6e865295798126ce5bdbd58a1` **tras** la corrección del
comentario de R25, que se hizo después de todas las mutaciones y no las afecta)
(antes de esta tanda: `1cd40aba…` y `3ba02ba8…`, las filas «después» de las secciones de arriba).

## Verificación

```
$ pnpm exec tsc --noEmit
EXIT=0                                    (sin salida)

$ pnpm exec eslint "app/(app)/mis-asignaciones/_components/RepartoModule.tsx" \
    "app/(app)/mis-asignaciones/_components/pos-card/pos-estado.ts" \
    "tests/components/RepartoAyuda.test.tsx"
EXIT=0                                    (sin salida)

$ pnpm exec vitest run tests/components/RepartoAyuda.test.tsx tests/components/RepartoModule.test.tsx \
    tests/components/PosOrderCardSinNotaPrivada.test.tsx tests/components/RecogerModule.test.tsx \
    tests/components/MarcarLuegoToggle.test.tsx
 Test Files  5 passed (5)
      Tests  132 passed (132)

$ pnpm exec vitest run tests/unit/guards
 Test Files  57 passed (57)
      Tests  834 passed (834)
```

Los cinco de componentes son los que renderizan alguna de las tres vistas POS (las dos que consumen
`estadoBadgeClass` y el módulo entero); las guardias van enteras porque **leen el fuente**, y este
cambio toca comentarios de `RepartoModule.tsx`. Del gate completo se encarga el leader.

**Sin commit.** Archivos: `pos-card/pos-estado.ts`, `RepartoModule.tsx`,
`tests/components/RepartoAyuda.test.tsx`, `progress/impl_235_m4.md` y este archivo.
