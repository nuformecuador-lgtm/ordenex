# impl 271 — R42: el RECHAZO avisa al mensajero de que quedó bloqueado (BACKEND)

> Cierra la ausencia n.º 2 de `progress/impl_271.md` («R42 — el aviso de RECHAZO está a medias»).
> Rama: `feature/271-r42-aviso-rechazo`, partiendo de `9d1b9e03`.
> **Alcance: sólo el cableado del productor.** No se tocó `db/`, ni el enum, ni el emisor, ni los
> textos: todo eso ya existía y estaba probado.

---

## El agujero que se cierra

El evento (`mensajero_bloqueado_por_cierres`), el emisor (`emitirMensajeroBloqueado`), el
notificador (`notificarMensajeroBloqueadoCon` / `...Real`) y los textos ya estaban en el árbol y en
verde desde `9d1b9e03`. Lo que **no** existía era quien lo dispara al rechazar: `CierresAdminService`
no recibía notificador, así que el rechazo era **mudo**.

Y era el silencio que más importa: **el rechazo es la única vía por la que un mensajero llega a
tener DOS cierres re-solicitables** (solicita el día 1, solicita el día 2, el admin rechaza los
dos → `N=2, V=2`). El caso más confuso de todos era justo el que no avisaba; el mensajero se
enteraba de que le habían rechazado el cierre **al toparse con el bloqueo** al día siguiente.

---

## Archivos modificados

| Archivo | Qué cambia |
| --- | --- |
| `lib/services/CierresAdminService.ts` | 6.º parámetro de constructor `notificarBloqueo: MensajeroBloqueadoNotificador = notificadorNoOp`; `findBloqueoDetalle` entra en su `Pick<IOrdenRepository, …>`; nuevo privado `avisarBloqueoPorRechazo`; llamada en la rama `updated` de `rechazarCierre` |
| `lib/actions/cierres-admin.ts` | COMPOSITION ROOT: inyecta `notificarMensajeroBloqueadoReal` |
| `tests/unit/services/notificacion-notificadores-reales.test.ts` | `CierresAdminService.ts` entra en el censo `SERVICES_CON_NOTIFICADOR`; + guardia «`lib/actions/cierres-admin.ts` inyecta el notificador real»; el título del test de defaults pierde el número (decía «los CINCO» con seis en la lista) |

## Archivo creado

| Archivo | Qué es |
| --- | --- |
| `tests/unit/services/cierres-admin-aviso-rechazo.test.ts` | 18 casos: emisión, entidad, orden, los cinco desenlaces que NO avisan, R47 por partida doble y el default no-op |

**Nada en `db/`, nada en `app/`, ninguna migración, ningún texto tocado.**

---

## El patrón, que es el de la casa y no uno nuevo

Copiado de `CierreDiaService.avisarCierrePorAprobar` / `avisarBloqueoPorAcumular` y de
`CorteDiarioService` (el cableado más fresco, del mismo commit):

1. **Notificador INYECTADO por constructor, con default `notificadorNoOp`.** Este service lo
   instancian **trece suites** y al otro lado hay una base local **compartida**: con el real por
   defecto, cualquiera de ellas escribiría avisos de verdad. El real se cablea **sólo** en el
   composition root.
2. **DESPUÉS de la escritura ya confirmada** (`res === "updated"`), nunca antes.
3. **FUERA de la `$transaction`**: a `resolverCierre` no le llega nada del aviso. En Postgres un
   error de sentencia aborta la transacción entera, así que un aviso caído **revertiría un rechazo
   legítimo** — la lección que la 262 dejó escrita en `notificarDiaRepartoCorregidoCon`.
4. **BEST-EFFORT** (`emitirBestEffort`): el fallo se absorbe **y se registra con su causa** (no es
   un `catch` vacío). El rechazo manda, el aviso es cortesía (R47).
5. **La entidad de la notificación es el cierre RECHAZADO**, no el mensajero ni el
   `aResolverPrimero`: dos rechazos son dos `entidad_id`, luego `notificacion_dedupe_key` no
   colisiona y el segundo rechazo **sí** avisa aunque el primero siga sin leerse (R44).

### Las dos decisiones que no son obvias

- **De dónde salen el mensajero y la zona.** `rechazarCierre` recibe un `cierreId` y un actor que es
  el **ADMIN**; los dos destinatarios del aviso viven en la fila del cierre. Se relee con
  `findCierreByIdEnAlcance` y **con el mismo `alcance` que acaba de autorizar la escritura**, así
  que el aviso no puede alcanzar un cierre que ese admin no podía tocar. Es una lectura de más en
  un camino que un humano ejecuta a mano y raras veces —la pantalla acaba de correr esa misma
  consulta para pintarle el detalle— y **evita abrir una consulta nueva en el repositorio**, que
  necesitaría su propio test contra Postgres y no es lo que este diff viene a hacer.
- **La guarda `if (!bloqueo.bloqueado) return`.** Tras un rechazo `V ≥ 1` casi siempre, pero R16
  permite re-solicitar **SIEMPRE** (anti-deadlock), así que la carrera existe. Un aviso que promete
  menos de lo que el servidor acepta es el fallo que R43 prohíbe por su nombre.

---

## Mapa `R<n>` → test

Todos en `tests/unit/services/cierres-admin-aviso-rechazo.test.ts` salvo donde se indique.

| R | Qué exige | Test |
| --- | --- | --- |
| **R42** | El rechazo avisa al mensajero | «emite UNA vez, al mensajero de la fila y a la zona DESTINO del cierre» |
| **R42** | …y sólo cuando el rechazo OCURRE | los cinco casos de «sin rechazo NO hay aviso»: `conflict`, `fuera_de_alcance`, motivo vacío, rol no autorizado, `adminSatelite` sin zona |
| **R42** | El caso que la motiva | «dos rechazos seguidos avisan DOS veces, con entidades distintas» |
| **R42** | Cableado en producción | `notificacion-notificadores-reales.test.ts` → «lib/actions/cierres-admin.ts inyecta el notificador real» + el censo contra el árbol |
| **R43** | El aviso CUENTA (N, V y cuál primero) | «el aviso viaja con el DETALLE del bloqueo, no con un booleano»; los literales siguen probados en `bloqueo-textos.test.ts` |
| **R43** | No promete lo que el servidor rechaza | «si el mensajero YA no está bloqueado (re-solicitó en medio), no se le dice que lo está» |
| **R44** | Hecho nuevo → aviso nuevo | «la ENTIDAD del aviso es el cierre RECHAZADO, no el que toca resolver primero» |
| **R47** | Un aviso caído no tumba la operación | «un aviso que LANZA no cambia el resultado» (+ que el fallo queda **registrado**) y «si la RELECTURA del cierre lanza, el rechazo tampoco se cae» |
| **§ tx** | El aviso no entra en la transacción del dinero | «`resolverCierre` NO recibe notificador ni nada del aviso» (afirma las claves EXACTAS del input) + «el orden es: escribir el rechazo → releer → avisar» |
| **§ default** | Sin inyectar no se emite nada | «construido sin inyectar, un rechazo termina en ok y no escribe ningún aviso» (recorre el camino entero y no emite ni registra) |

---

## Mutaciones — arnés manual, con autocomprobación

La suite **sin mutar** verde primero; cada mutación aplicada, **vitest ejecutado de verdad**,
revertida; y al final todo verde otra vez. Salida real, una línea por corrida:

```
SIN MUTAR                                                  Tests  18 passed (18)
[1] el aviso sale del `if (res === "updated")`             2 failed | 16 passed  -> `conflict` no avisa; `fuera_de_alcance` no avisa
[2] la entidad pasa a ser `aResolverPrimero.cierreId`      2 failed | 16 passed  -> la ENTIDAD es el cierre RECHAZADO; dos rechazos, dos entidades
[3] fuera `emitirBestEffort` (el aviso propaga)            2 failed | 16 passed  -> los dos casos de R47
[4] el default pasa a ser `notificarMensajeroBloqueadoReal` 2 failed | 18 passed -> (censo) defaults no-op; censo COMPLETO
[5] fuera la guarda `if (!bloqueo.bloqueado)`              1 failed | 17 passed  -> «si el mensajero YA no está bloqueado…»
[6] el aviso se emite ANTES de `resolverCierre`            8 failed | 10 passed  -> emisión, orden, relectura, R47, los dos «no avisa», default
[7] algo del aviso viaja DENTRO del input de la tx         1 failed | 17 passed  -> «`resolverCierre` NO recibe nada del aviso»
COMPROBACION FINAL: sin residuo (`grep MUTACION` vacío)    Tests  18 passed (18)
```

**7 de 7 mutaciones muertas.** La [4] la mata la guardia del censo, no esta suite: es el único
mecanismo que impide que el default vuelva a ser el notificador real.

---

## Verificación ejecutada

**NO se corrió `./init.sh`**: hay otro agente trabajando en paralelo sobre esta feature y la base
local es compartida (el gate completo lo lanza el leader al final). Se corrió lo que este diff toca,
todo con **dobles y sin base de datos**:

```
> npx tsc --noEmit                       (= pnpm run typecheck)
TYPECHECK exit=0        (sin una sola línea de salida)

> npx eslint lib/services/CierresAdminService.ts lib/actions/cierres-admin.ts \
             tests/unit/services/cierres-admin-aviso-rechazo.test.ts \
             tests/unit/services/notificacion-notificadores-reales.test.ts
LINT exit=0             (sin una sola línea de salida)

> npx vitest run <las 16 suites que tocan CierresAdminService o los notificadores>
 Test Files  16 passed (16)
      Tests  286 passed (286)

> npx vitest run tests/unit/notificaciones tests/unit/guards tests/unit/utils/bloqueo-cierre.test.ts
 Test Files  73 passed (73)
      Tests  1071 passed (1071)
```

Las 16 suites: `cierres-admin-aviso-rechazo`, `cierres-admin-completo`,
`cierres-admin-confirmacion-fisica`, `cierres-admin-corregir-pagos`,
`cierres-admin-historico-paginado`, `cierres-admin-indemnizacion`, `cierres-admin-pendiente`,
`cierres-admin-pendientes-paginado`, `cierres-admin-service`,
`CierresAdminService.aprobar.devolucion`, `CierresAdminService.gestiones-completo`,
`indemnizacion-tope-negocio-cierre`, `notificacion-notificadores-reales`,
`notificacion-productores-wiring`, `bloqueo-textos`, `tests/integration/actions/cierres-admin-action`.

**El worktree no traía `node_modules` ni `.env`**: se resolvió con un *junction* al del repo
principal (sin `prisma generate`, que se pisa entre worktrees). Ninguna de las suites de arriba
necesita base.

---

## Lo que sigue SIN cubrir de R42, y lo que encontré de paso

1. **`tests/integration/db/devolucion-rechazadas-flow.test.ts` no se ejecutó** (necesita Postgres y
   la base local está compartida con el otro agente). Construye `CierresAdminService` con cinco
   argumentos, así que el parámetro nuevo —opcional y el sexto— no lo altera; el typecheck lo cubre.
2. **No hay test contra Postgres de este aviso.** No hace falta uno nuevo: este diff **no añade ni
   una consulta**; reusa `findCierreByIdEnAlcance` y `findBloqueoDetalle`, que ya tienen los suyos
   (`cierre-bloqueo-nv-sql-real.test.ts`). Lo que aquí se prueba es orquestación, y los dobles la
   ven entera.
3. **R44 sigue sin su test de las dos mitades** contra la clave real `notificacion_dedupe_key` —era
   ya la ausencia n.º 4 de `progress/impl_271.md` y este diff no la cierra. Lo que sí queda
   afirmado es la mitad de la que depende: **la entidad es el cierre**, no el mensajero.
4. **La pantalla no cambia.** El aviso sale por la campana; que la fila del admin pinte el bloqueo
   (R48) sigue siendo trabajo de la tanda 9 (frontend).

### ⚠️ HALLAZGO FUERA DE MI ALCANCE — R38/R39 están muertos en producción

`notificarCierreDiaVencidoReal` **no lo importa nadie**:

```
grep -rn "notificarCierreDiaVencidoReal" --include=*.ts . | grep -v node_modules
  ./lib/notificaciones/notificadores.ts:...   (sólo su propia definición)
```

`app/api/cron/corte-diario/route.ts` construye el `CorteDiarioService` con **cinco** argumentos y
**no** le inyecta el notificador, así que el corte usa su default **no-op**: el aviso de «tu cierre
del día venció» —el que más se emite, R38/R39— no se emitirá nunca en producción, con toda la suite
en verde. Es exactamente el agujero que el bloque de guardias de
`notificacion-notificadores-reales.test.ts` existe para cerrar, y que no lo cerró porque **allí las
guardias de composition root son una por archivo escrita a mano** y a `CorteDiarioService` sólo se
le añadió la entrada del censo, no su guardia de raíz.

**NO lo he tocado** (es de la pasada del otro agente y toca `app/`), y **no he añadido su guardia**
porque dejaría la suite roja en un punto que no me corresponde arreglar. Se reporta para que el
leader decida: son dos líneas en el `buildService()` de esa ruta, más una guardia gemela a la que sí
añadí para `cierres-admin.ts`.

### Nota sobre `progress/impl_271.md`

Su fila de R42 («**PARCIAL** — el productor del rechazo no está cableado») y su ausencia n.º 2
quedan **obsoletas** con este commit. No las edité a propósito: el otro agente puede estar
escribiendo en ese mismo archivo y un conflicto ahí no aporta nada.

---

**Veredicto:** R42 cerrado —productor cableado con el patrón de la casa, fuera de la transacción del
dinero y sin poder alterar el rechazo—, 18 casos nuevos, 7 de 7 mutaciones muertas, typecheck y lint
limpios; queda reportado que R38/R39 no están cableados en el cron del corte.
