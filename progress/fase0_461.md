# Ficha 461 — Fase 0: caracterización de lo que NO puede cambiar

- Fecha: 2026-09-25. Rama `feature/461-backend` nacida de `origin/dev` @ `f415f4bb` (contiene el spec).
- Base: clon `ordenex_461` (`CREATE DATABASE ordenex_461 TEMPLATE ordenex`), `prisma migrate status`:
  «ordenex_461 at localhost:5432 · 216 migrations found · Database schema is up to date!».
- Entorno propio: `pnpm install --frozen-lockfile` (35 s, exit 0, sin junction) + `prisma generate`.
- Búsqueda: MCP `codebase-memory` (`R-job-singularis-projects-ordenex`) consultado primero; el índice
  está RANCIO para la 459 (no devuelve `CobroTiendaService` ni `CajaPagoPorCuentaFeedService`, y sitúa
  `derivarCaja` en `caja-tesoreria.ts:197-227` cuando hoy está en `:272-323`). Todo lo de esta ficha se
  confirmó leyendo el archivo real.

## T0.1 — La fotografía y la invariante sobre el SHA de `dev` (R59)

Comando: `pnpm exec vitest run tests/integration/db/caja-caracterizacion-459.test.ts
tests/integration/db/caja-invariante-tiendas.test.ts --reporter=verbose`

```
 Test Files  2 passed (2)
      Tests  20 passed (20)      ← 0 skipped (HAY_BASE_DE_DATOS = true contra ordenex_461)
VITEST_EXIT=0
```

### Literales de HOY (sobre `f415f4bb`, antes de tocar una línea)

Los que la 461 **mueve a propósito** (design §14.1) y los que **no deben moverse**:

| Cifra (Δ del escenario sobre el libro entero) | Hoy | Tras la 461 (a mano, §14.1) |
| --- | --- | --- |
| `ganancia` | **−89 874,84** | −87 374,34 (+2 500,50: el cobro pasa a ser cargo propio) |
| `deTerceros` | **15 546,32** | 13 045,82 (−2 500,50 = Σ saldos 7 530,70 + 5 515,12) |
| `enCaja` (cifra principal) | −74 328,52 | **sin cambio** |
| `entradas` («Entró») | 87 517,25 | **sin cambio** |
| `salidas` («Salió») | 161 845,77 | **sin cambio** |
| `capital` / `deOrdenex` | 0,00 / −89 874,84 | 0,00 / −87 374,34 |
| `ingresosPropios` / `egresosPropios` | 63 970,93 / 153 845,77 | 66 471,43 / 153 845,77 |

Saldos y desgloses de las tiendas (no cambian con la 461):

- Tienda A: saldo 7 530,70 · aFavor 19 900,00 · cargos 7 369,30 · pagado 5 000,00.
- Tienda B: saldo 5 515,12 · aFavor 16 617,00 · cargos 8 101,88 (incluye el cobro 2 500,50) · pagado 3 000,00.
- Mensajero: devengado 10 500,00 · pagado 8 664,00 · cuenta por pagar 1 836,00.

Filas del libro de la caja (22 hoy; tras la 461 serán 23: gana `cobro_tienda|ingreso|ingreso_cobro_tienda|2500.50`):
las de `caja-caracterizacion-459.test.ts:305-335`. Filas del libro de la tienda B (9, sin cambio):
`manual|debito|cobro_manual|2500.50` entre ellas. Composición de la ganancia hoy: 7 filas de ingreso
(`ingreso_ajuste` 51 000,25 la mayor), `egreso_pago_mensajero` 9 500,00, `egreso_ajuste` 500,10, otros 0,00.

Invariante (`caja-invariante-tiendas.test.ts`) hoy: R8 con excepción `Δ deTerceros = Σ saldos +
cobrosNoReclasificados` (2 500,50 antes de reclasificar, 0,00 después); 8 pasos, cada uno con 1 fila nueva
en la caja salvo el escenario (22).

## T0.2 — Mutaciones medibles HOY (control del arnés, R59/R60)

Ejecutor: `scratchpad/mutar.sh` — aplica UNA línea con `sed`, exige `git diff` no vacío, corre SOLO los
tests indicados, revierte con `git checkout -- <archivo>` y exige `git diff --stat` vacío. Las 14 de design
§14.2 nombran código que aún no existe (`emitirCargoDeCobro`, `ingreso_cobro_tienda`…): se corren al cerrar
cada bloque y en el anexo T Z.1 (backend: 1–10 y 14; las 11–13 son del `frontend_dev`).

| # | Mutación (diff de una línea) | Tests corridos | Resultado |
| --- | --- | --- | --- |
| M0-1 | `PagoPorCuentaTiendaService.ts:209` — `await this.caja.emitirEgresoDePagoPorCuenta(tx, {` → `if (tx === null) await …` (control positivo: pago por cuenta sin salida en la caja) | caja-invariante-tiendas, caja-caracterizacion-459, pago-por-cuenta-tienda (int), pago-por-cuenta-tienda-service | **ROJO 11 de 55** (3 archivos): invariante «anti-vacuidad» (pago por cuenta escribe 0 filas), «R8 · pago por cuenta: expected '15546.32' to be '14311.76'», «R39/R48: expected enCaja '0.00' to be '-1234.56'»; integración R29/R41/R46; unitario R29/R42/R35/R43/R56. La fotografía queda VERDE porque su escenario no ejerce el pago por cuenta (ese camino vive en la invariante). |
| M0-2 | `desglose-tienda.ts:50` — `cobro_manual: "cargos"` → `"aFavor"` | caja-caracterizacion-459, desglose-tienda | **ROJO 6 de 38**: fotografía «R92: saldo y desglose de la tienda B» (cargos ≠ 8 101,88); `desglose-tienda.test.ts` 381/R36, R26 («expected '11500.00' to be '8500.00'»), R27, acumulación, R8 cubeta↔tipo |
| M0-3 | `LiquidacionService.ts:1234` — contra-asiento del pago a tienda `categoria: "ajuste_credito"` → `"ajuste_debito"` | caja-caracterizacion-459, caja-invariante-tiendas | **ROJO 20 de 20**: el CHECK `wallet_tienda_movimiento_tipo_categoria_check` rechaza `credito`+`ajuste_debito`, `anularPagoTiendaA` lanza y CADA caso cae con su nombre (la siembra no se traga el error) |
| M0-4 | `PagoPorCuentaTiendaService.ts:276` — `monto: pago.monto` → `monto: "1.00"` en el crédito de la anulación (la petición no lleva monto: `.strict()`; la sustitución es por una constante) | caja-invariante-tiendas, pago-por-cuenta-tienda (int), pago-por-cuenta-tienda-service | **ROJO 4 de 41**: unitario «R46/R47/R42 … con el monto DEL DOCUMENTO»; integración «R46/R47/R48/R53 … expected '7655.40' to be '20000.00'»; invariante «R8 · anulacion del pago por cuenta: expected '15546.32' to be '14312.76'» y R39/R48 |
| M0-5 | `caja-tesoreria.ts:112` — `ingreso_flete: "cargo_a_tienda"` → `"efectivo"` | caja-caracterizacion-459, caja-invariante-tiendas, caja-clasificacion-459.guardia, caja-derivacion-459 | **ROJO 10 de 43**: guardia (4) «cargos a tienda ≠ los seis»; `caja-derivacion-459` R2/R3 («expected '33431224.00' to be '29059224.00'»), R5, R5/R8, R6/R7, R74/R75; fotografía bloque «a propósito» («entradas '95517.25' ≠ '87517.25'»); invariante «R8 · escenario: expected '23546.32' to be '15546.32'» |

Tras cada una: `git diff --stat` vacío. Ninguna superviviente. Logs completos en el scratchpad de la
sesión (`mut/M0-*.log`, `mut/M0-*.vitest.log`).

## T0.3 — Contraste de línea base en producción

Del LEADER (MCP de Supabase, solo lectura): C461-0, C461-1, C461-2, C461-3 y C5 → `progress/contraste_461.md`.
No se corrió desde este worktree.

## T0.4 — Gate y commit

Bloque 0 no toca código de producción (solo esta nota). El gate COMPLETO se corre UNA vez al cerrar el
bloque B contra `ordenex_461` (`progress/gate_461_backend.log`, con `INIT_EXIT=$?` dentro), y cubre 0 + A + B;
la fotografía y la invariante ya corren verdes aquí sobre el SHA de `dev`. Commit: `docs(461): fase 0`.
