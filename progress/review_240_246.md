# Revisión conjunta — 240 (rechazo manual de la tienda) y 246 (asignación por día)

- **Rama:** `docs/240-246-247-specs` · **Commit revisado:** `9b5bb19d` sobre `origin/dev` = `19f74829`
- **Reviewer:** subagente reviewer · **Fecha:** 2026-08-20
- **Modo:** revisión conjunta, porque las dos fichas se desarrollaron sobre el mismo árbol y comparten
  bordes (`lib/notificaciones/emitir.ts`, repositorio del cierre, catálogo de acciones de `/novedades`).

> ⚠️ Este acta se escribe **de forma incremental**. El veredicto de la cabecera es **provisional**
> hasta la línea `## VEREDICTO FINAL`. Si el acta termina sin esa sección, la revisión se cortó.

## VEREDICTO (cerrado — el detalle y el remite están en `## VEREDICTO FINAL`, al final)

- **240 — rechazo manual de la tienda:** **RECHAZADO** · 2 bloqueantes acotados (R24 sin evidencia;
  `tasks.md` en blanco con tareas realmente sin hacer). La sustancia pasa.
- **246 — asignación por día:** **OK** · sin bloqueantes. Queda fuera: marcar `tasks.md`, re-medir
  los planes con volumen, y la entrada en `progress/history.md`.

## Estado de partida verificado

- `git status` limpio salvo `progress/recorrido_240_246.md` (sin seguimiento, aportado por el humano).
- `./init.sh` **no se re-corre** en esta revisión: el humano ya lo corrió completo con el árbol quieto
  (EXIT 0, 1241 archivos / 16.332 tests / 359 s). Lo que sí se hace aquí es **correr a mano** los
  tests concretos de cada requisito y **matarlos con mutaciones**, una a una, restaurando entre cada
  una y dejando sha256 antes/mutado/después.

---

## 1 · 💰 La paridad con el cron (240/D1) — VERIFICADA, y matada con dos mutaciones

**Lectura del código, lado a lado.** `GestionOrdenRepository.rechazarDesdeDevuelta` (vía el helper
`transicionarDesdeDevuelta`, `lib/repositories/GestionOrdenRepository.ts:307-378`) escribe lo mismo
que `DevolucionSlaRepository.escalarDevueltaSla` (`:175-218`): `updateMany` guardado por
`{id, estatusId: devuelta, deletedAt: null}` con `data` de UNA clave, gestión `resultado:
"rechazada"` con `cierre_id NULL` atribuida al mensajero de la última `devuelta` vigente, y append
por el choke point. Lo único que difiere: `origen_tipo` (`rechazo_tienda` vs `escalado_devuelta_sla`),
el `actorUsuarioId` (la tienda vs `null`) y que aquí el motivo es obligatorio. **Ninguna aritmética
nueva**: el importe cuelga del `resultado`, que es el mismo.

### Mutación REV-1 — 💰 el rechazo manual SIN gestión sintética (rechazar a mano sale gratis)

`lib/repositories/GestionOrdenRepository.ts`, el `crearGestion` del rechazo sustituido por
`async () => ({ id: "MUTANTE-sin-gestion-sintetica" })`: la transición ocurre, la gestión NO.

```
SHA ANTES:   a7d0bf4e484c98244cbe30ce4c704a877cf96cee0f7aee5d479f39df6a466733
SHA MUTADO:  a3426dec03f4ee7f891a713b033c25687fef24478cd6424fbdb63857c3941524
SHA DESPUES: a7d0bf4e484c98244cbe30ce4c704a877cf96cee0f7aee5d479f39df6a466733
```

Muerta: `Tests 6 failed | 62 passed (68)`, las seis en
`tests/unit/repositories/gestion-orden-rechazar.test.ts` (R8/R18, R9, R11/R12, R15, R16, R5/R21).

⚠️ Los tres archivos de servicio y de borde siguieron VERDES —`rechazo-tienda-service.test.ts`,
`resolver-novedad.test.ts`, `gestion-orden-reprogramar.test.ts`—: el repositorio es el ÚNICO que ve
el efecto de dinero. Es otra vez la lección medida del repo, y esta vez el test está donde toca.

### Mutación REV-2 — 💰 la carrera con el cron: quitar la guarda de estado del UPDATE

Borrada la línea `estatusId: input.estatusDevueltaId` del `where` compartido.

```
SHA ANTES:   a7d0bf4e484c98244cbe30ce4c704a877cf96cee0f7aee5d479f39df6a466733
SHA MUTADO:  75511391b5d405cb21b944d01ee9c1923fdd876509b51f142b59412d055a637a
SHA DESPUES: a7d0bf4e484c98244cbe30ce4c704a877cf96cee0f7aee5d479f39df6a466733
```

Muerta: `Tests 4 failed | 35 passed (39)` — 3 casos del rechazo (R3, R5/R21, R4) y 1 de la
reprogramación de la 100, que comparte el helper. El sha del mutante coincide con el que el
implementer reportó en T7.1: la mutación es la misma y el resultado se reproduce.

**Conclusión.** La carrera con el cron no puede duplicar el cobro: el segundo en llegar obtiene
`count = 0`, sale antes del paso 2 y no crea gestión ni historial. Ese camino tiene tres casos que
lo afirman con un doble que evalúa el `where` clave a clave y que se autocomprueba
(`tests/unit/repositories/gestion-orden-rechazar.test.ts:312-332`). **Sin hallazgos.**

---

## 3 · 💰 El ancla del corte (246) — FIJADA Y MATADA

`lib/services/CorteDiarioService.ts:64-66`: `diaQueElCorteCierra(now) = startOfDayCR(now) - 24 h`.

### Mutación REV-3 — el ancla pasa a ser «hoy»

```
SHA ANTES:   3c22ea5d150eec66e28ed1726e185508d1928986c04f0b5268396f581aab546b
SHA MUTADO:  cdc97ce74233762188ae79586a93274ee18f46b6ec7e3b2ef1646b23c119c035
SHA DESPUES: 3c22ea5d150eec66e28ed1726e185508d1928986c04f0b5268396f581aab546b
```

Muerta: `Tests 9 failed | 124 passed (133)`, en dos archivos. Entre ellas, las tres que fijan el
ancla por su nombre: «EL ANCLA: corriendo a las 00:00 CR del 21, el dia que se cierra es el 20 — no
el 21», «si el cron se ADELANTA a las 23:5x CR del 20, cierra el 19» y «cada corrida avanza su ancla
un dia: la proteccion caduca sola»; más las dos de R13. El punto por el que la ficha se rompía sola
está atornillado. **Sin hallazgos.**

---

## 4 · 💰 El peligro del día del despliegue (246) — T7.4 CORRIDA POR MÍ

`lib/repositories/RankingRepository.ts:78-86`, el `OR` de dos ramas disjuntas.

### Mutación REV-4 — se quita la rama de respaldo

```
SHA ANTES:   fd8a11c7ce15ce7b3a357369e0b08e17da5b269a8cd53e2e403fd5489007e801
SHA MUTADO:  421afd53d5b87f496e4e20e4df6b3920c93e8a8b298037e97664e2c8c1c74dac
SHA DESPUES: fd8a11c7ce15ce7b3a357369e0b08e17da5b269a8cd53e2e403fd5489007e801
```

- Tests de SERVICIO: `Tests 65 passed (65)` — **VERDES**. Confirmado lo que el autor avisó: la suite
  de servicio no ve el `WHERE`.
- Test de REPOSITORIO: `Tests 3 failed | 9 passed (12)` — muerta por «R37/R43: una orden SIN dia de
  reparto cuenta por `asignado_at` — el respaldo», «R43: MEZCLA del dia del despliegue — sin salto
  artificial en el denominador» y «el `where` tiene la forma de DOS ramas».

El test vive donde vive el `WHERE`, su doble evalúa las dos ramas de verdad (hay un caso «el doble
detecta lo que dice detectar (autocomprobacion de las dos ramas)») y hay un caso de disyunción
(«RAMAS DISJUNTAS: ninguna orden se cuenta DOS veces en dos dias distintos»). **Sin hallazgos.**

---

## 5 · El índice — PREFIJO CONSERVADO Y `down.sql` QUE REPONE

- `db/migrations/20260820180000_orden_fecha_reparto/migration.sql` amplía el compuesto:
  `CREATE INDEX "orden_mensajero_asignado_at_fecha_reparto_idx" ON "orden" ("mensajero_asignado_id",
  "asignado_at", "fecha_reparto")` y DESPUÉS `DROP INDEX IF EXISTS
  "orden_mensajero_asignado_id_asignado_at_idx"`. Crear-antes-de-borrar: no hay ventana sin índice.
- **El prefijo se conserva**: las dos primeras columnas, en ese orden, son las del índice de la 76.
  Atornillado en `tests/integration/db/orden-fecha-reparto-migration.test.ts` («R44: el PREFIJO se
  conserva…», con la igualdad literal de las tres columnas) y «el numero de indices de `orden` NO
  sube — se SUSTITUYE».
- **El `down.sql` repone el de dos columnas** con su nombre exacto, ANTES de soltar la columna, y con
  `IF EXISTS`/`IF NOT EXISTS` en las tres sentencias. Tiene caso propio («R21: REPONE el indice de
  dos columnas — no se limita a soltar la columna») y otro que exige que las dos consecuencias
  operativas estén escritas en el `down`.
- **Sin drift con Prisma:** `db/schema.prisma:603` declara
  `@@index([mensajeroAsignadoId, asignadoAt, fechaReparto], map: "orden_mensajero_asignado_at_fecha_reparto_idx")`
  y ya no declara el de dos columnas; hay un caso que lo comprueba en las dos direcciones.

Corridos por mí, los 6 archivos de migración y guardias implicados: `Tests 229 passed (229)`.

---

## 6 · La guardia de dinero que lo paró — QUEDÓ MÁS ESTRICTA

`tests/unit/tablero-dia/asignado-at-solo-lectura.guardia.test.ts`. La lista blanca pasa de 2 a 4
entradas (el `migration.sql` y el `down.sql` de la 246) y **a cambio** entra el caso «246: el permiso
nuevo es SOLO para indexar», que exige, sentencia a sentencia, que toda aparición de `asignado_at` en
SQL vivo de esas dos entradas caiga dentro de un `CREATE INDEX`/`DROP INDEX`, más su autocomprobación
en las dos direcciones. No se relajó el censo (que habría sido enseñarle que «un `CREATE INDEX` no
toca la columna»): se concedió un permiso **acotado**.

### Mutación REV-5 — plantar una escritura dentro de la migración autorizada

Añadido `UPDATE "orden" SET "asignado_at" = NOW() WHERE "fecha_reparto" IS NOT NULL;` al
`migration.sql` de la 246.

```
SHA ANTES:   312f02ab964a7721e3be89c3be3ee76f601d6510f517bd297faac46ea9763676
SHA MUTADO:  e9fdf3dbd39d575b330e63314b4fb7dc048e83ca530377f1f45108c2a41018df
SHA DESPUES: 312f02ab964a7721e3be89c3be3ee76f601d6510f517bd297faac46ea9763676
```

Muerta por el caso NUEVO: `FAIL … 246: el permiso nuevo es SOLO para indexar` → «toca `asignado_at`
fuera de un indice». El permiso no es un cheque en blanco. **Sin hallazgos.**

---

## 2 · La guardia anti-maqueta (240/D3) — SE PONE ROJA, PERO HAY UNA QUINTA FORMA QUE SOBREVIVE

### Lo que sí hace (comprobado, no recitado)

`tests/unit/guards/novedad-acciones-sin-maqueta.guardia.test.ts` (611 líneas) tiene los cuatro
frentes anunciados, más un bloque 0 de autocomprobación de sus propios detectores, más tres casos
anti-vacuidad (el censo del árbol, el mapa literal `acción ← archivo`, y el censo inverso «el
detector VE de verdad las acciones de la pantalla»). Corrida: verde. La lista de exentos
`NO_SON_ACCIONES_DE_FILA` tiene además su caso de caducidad («todo lo que exime, la pantalla lo
dispara HOY»), que es lo que impide que se convierta en la allowlist que la 240 no quería.

### 🔴 hallazgo · La quinta forma de replantar la maqueta — MEDIDA

**El frente 2 mide que la pantalla IMPORTA el productor, no que lo LLAME.** Su propio mensaje de
error dice «declara una operación que NINGÚN archivo de la pantalla **llama**», y eso es más de lo
que el detector comprueba: `importaElSimbolo()` sólo mira las aristas de `import`.

Mutación REV-6, sobre `app/(app)/novedades/_components/RechazarNovedadModal.tsx`: se deja el
`import { rechazarNovedad }` **intacto** y se sustituye la invocación por una respuesta fabricada.
El botón vuelve a ser una maqueta: se pulsa, no ocurre ninguna operación.

```
SHA ANTES:   520cd779c8428827125034675a1c9425d8fbb523d6022aa834124d4e7b03716f
SHA MUTADO:  3ef008699963039f103fe8e79539e79198e4f2fedbd722e2eb0300989284ed7d
SHA DESPUES: 520cd779c8428827125034675a1c9425d8fbb523d6022aa834124d4e7b03716f
```

- `novedad-acciones-sin-maqueta.guardia.test.ts` + `superficie-de-uso.guardia.test.ts`:
  **`Tests 33 passed (33)` — LAS DOS VERDES con la maqueta puesta.**
- Quien la mata es `tests/components/RechazarNovedad.test.tsx`: `Tests 4 failed | 96 passed (100)`.

**Y la variante ingenua también pasa las dos guardias.** Mutación REV-6b: se quita la llamada y se
deja el import huérfano (sha `739c8f0486bbe1d9d3d2f69e09d48c5853ea10b38d0ef1cad60c12b2e664f2d9`,
restaurado a `520cd779…`). Las dos guardias: `Tests 33 passed (33)`. `pnpm exec eslint` sobre ese
archivo devuelve **`1 problem (0 errors, 1 warning)`** —`'rechazarNovedad' is defined but never
used`— y el script del repo es `"lint": "eslint"`, **sin `--max-warnings=0`**: el gate de lint
tampoco lo para.

**Por qué importa y por qué no es un `BLOQUEANTE`.** El argumento de D3 contra la opción (c) era que
un test de componente «se cambia junto al cableado y sigue verde». Con este hueco, la única red que
queda ante «el botón dejó de llamar a la acción» es **exactamente ese test de componente**. La
guardia sigue cerrando los cuatro modos que declara (productor inventado, cable cortado por
`import` borrado, excusa mentirosa, censo inverso) y la maqueta **muere en la suite**; lo que no
cubre es el import-sin-llamada. Se registra como **menor**, con el remedio barato: exigir en el
frente 2 que el símbolo aparezca además como **invocación** (`\brechazarNovedad\s*\(`) en alguno de
los archivos que lo importan, con su autocomprobación contra `SOLO_EL_TIPO`/`LA_MAQUETA` y contra un
fuente sintético que importe y no llame.

### Sobre el caso vacuo y el mutante equivalente que su autor ya encontró

Los dos están cerrados en el propio archivo y **verificados aquí**: el `SOLO_EL_TIPO` importa el
MISMO símbolo que se pregunta (si no, el `false` vendría del nombre y no del `import type`), y el
filtro del `type` en línea se observa con `expect(aristasDeImport(TIPO_EN_LINEA)[0].simbolos)
.toEqual(["reprogramarNovedad"])`, que es lo que lo saca de ser equivalente. No encontré un tercero
de esa clase.

---

## 7 · Los bordes compartidos — NINGUNA PISÓ A LA OTRA (con una salvedad heredada)

Revisados uno a uno los cuatro puntos de contacto:

| Borde | 240 escribe | 246 escribe | ¿Se pisan? |
| --- | --- | --- | --- |
| `lib/notificaciones/emitir.ts` | un párrafo (R45: `rechazo_tienda` no emite el aviso del destinatario) | — | **no**. Sólo comentario; el filtro no se toca, y tiene su test con control positivo |
| `lib/ranking/snapshot-dia.ts` | — | un párrafo (el matiz de las dos convenciones de `@db.Date`) | **no** |
| `lib/repositories/CierreDiaRepository.ts` | el `where` de familias pasa de igualdad a `in` (l. 348) | el `OR` del día en el pre-`SELECT` y en el `updateMany` (l. 529-580) y `fechaReparto: startOfDayCR()` en el deshacer (l. 898) | **no**: regiones distintas, cada bloque con su ficha citada |
| `lib/repositories/CierresAdminRepository.ts` | `FAMILIAS_DERIVADAS_DEL_HISTORIAL` + el `take` | `fechaReparto: null` en el handoff a bodega (l. 1392) | **no** |
| `app/(app)/novedades/_components/novedad-acciones-catalogo.ts` | la celda borrada + `PRODUCTOR_POR_ACCION` | — | **no**: la 246 no entra en `/novedades` |

**Ninguna lista perdió un valor.** `gestion-tienda-ayuda-flag.ts` → `gestion-de-la-tienda-flag.ts`
conserva `gestion_tienda_ayuda`, añade `rechazo_tienda` y **declara por escrito** la ausencia
deliberada de `reprogramacion_tienda`, con un caso que la afirma. Es lista blanca, con su motivo.

**Y una lista que la convivencia SALVÓ:** `GESTION_ADMIN_SELECT.historialEstados` tenía `take: 2`
literal con un comentario que explicaba por qué no podía ser `1`. Al pasar las familias a tres, ese
`2` habría **truncado en silencio** y `desdeAyudaTienda` habría salido `false` sobre gestiones que
**sí** registró la tienda — es decir, **deshacibles** por el mensajero. Se ató a
`FAMILIAS_DERIVADAS_DEL_HISTORIAL.length` y el test afirma **las dos cosas** (la derivación **y** el
`3` literal). Es un hallazgo real, no cosmético.

**Ningún test afirma lo de una y corre sobre lo de la otra.** Los tres archivos ajenos que la 246
tuvo que tocar (`ManifiestoFlujos`, `OrdenesRutearSatelite`, `incidente-admin-aislamiento`) llevan
cada cambio comentado con su ficha y su motivo; en `OrdenesRutearSatelite` se **conservó el
`toHaveBeenCalledWith` literal** en vez de degradarlo a `objectContaining`, que es justo la decisión
correcta cuando el literal ES el contrato.

**Salvedad, y es heredada de la 237, no de estas dos:**
`app/(app)/novedades/_components/NovedadAcciones.tsx:78` —archivo que la 240 **sí** edita— sigue
diciendo «un rechazo cobra a la tienda hasta ₡1.000». Los ₡1.000 son el `cobroRechazado`, que es
**ingreso de bodega**; lo que la tienda paga es el flete de devolución. Es exactamente la confusión
que D10 de la 240 corrigió en sus propios textos y que ya obligó a corregir el design de la 237.
Comprobado con `git show 19f74829`: la línea es **anterior** a este commit. **menor**, heredado.

---

## 8 · Trazabilidad — 47 + 46, cada cita comprobada

**Método, porque el aviso era explícito (cinco fichas seguidas citando tests inexistentes):** se
extrajeron por script **las 65 rutas de test** citadas en las cuatro bitácoras y se comprobó su
existencia en el árbol, y después **cada título de caso entre comillas angulares** del mapa se buscó
—normalizando tildes y comillas— en el corpus de `tests/`.

- **Rutas citadas: 65. Inexistentes: 0.**
- **Títulos citados: todos encontrados**, salvo tres que son **paráfrasis** del mapa y no citas
  literales, verificadas a mano una por una: 240/R18 (el caso real es «💰 R8/R18: crea UNA gestion
  `rechazada` con `cierre_id` nulo y su motivo»), 240/R33 (el caso real es un `it.each` con «grupo
  %s: …», que instancia «grupo devolucion: …») y 246/R1 en la fila que el backend marcó
  `[FRONTEND] pendiente` y que el frontend después cubrió.

**La racha de cinco fichas se rompe aquí: el mapa NO miente.**

**Ejecución independiente:** corrí **los 66 archivos de test que el commit toca**:
`Test Files 66 passed (66) · Tests 1602 passed (1602)`. Y `pnpm run typecheck` con el árbol
restaurado: **EXIT 0**.

### Los requisitos cuya evidencia NO es un test, dichos uno a uno

| Req | Qué hay | Veredicto |
| --- | --- | --- |
| **240/R24** (no re-anclar/borrar/modificar el anclaje) | **NADA.** `progress/impl_240.md` escribe «R24 \| — \| **NO CUBIERTO AQUÍ**. Es T8.3 (contra Postgres)», y **T8.3 no se hizo** | 🔴 **BLOQUEANTE** — ver hallazgos |
| **240/R47** (rollback ruidoso) | dos casos estáticos sobre el `down.sql` (recrea los 30 valores; falla ruidosamente si quedan filas). **El round-trip real no se corrió** | menor |
| **240/R22** (facturan lo mismo) | `ingreso-ordenex.test.ts` verde sin tocarse. El propio autor escribe que «su verde es coherencia, no evidencia» | menor, declarado |
| **246/R44** (el denominador se apoya en un índice) | **M8: cuatro `EXPLAIN` reales** citados con su salida + **4 casos** que atornillan la FORMA del índice. No hay test de plan, y el argumento —fingirlo sería una aserción contra su propia fuente— es la lección escrita de este repo | menor, con re-medición pendiente |
| **246/R30/R31/R32/R34** | guardias verdes sin tocarse; el autor declara que su verde es coherencia | menor, declarado |

**La diferencia entre 240/R24 y 246/R44, y por qué una bloquea y la otra no:** R44 tiene evidencia
—medida, citada y auditable— aunque no sea un test. **R24 no tiene ninguna**: ni test, ni medición,
porque la medición que lo cubría (T8.3) nunca se ejecutó.

---

## 9 · CHECKPOINTS.md, punto por punto

| Checkpoint | 240 | 246 |
| --- | --- | --- |
| `requirements.md` con EARS numerados | ✅ R1-R47 | ✅ R1-R46 |
| `design.md` con alternativa descartada y su porqué | ✅ (D1-D10, cada una con «qué se rompe con la otra») | ✅ (incluida la decisión del índice **invertida** con `EXPLAIN`) |
| `tasks.md` con **todas** las tasks `[x]` | ❌ **0 de 37** | ❌ **1 de 47** |
| Cada `R<n>` mapea a un test concreto | ❌ **R24 sin nada** | ⚠️ R44 por medición (aceptado) |
| `progress/impl_<f>.md` con el mapa `R<n> → test` | ✅ (backend + frontend) | ✅ (backend + frontend) |
| `pnpm run typecheck` | ✅ corrido por mí: EXIT 0 | ✅ ídem |
| `pnpm run lint` | ✅ (el gate del humano) ⚠️ el script es `eslint` sin `--max-warnings=0` | ✅ ídem |
| `pnpm test` | ✅ 66 archivos del commit, 1602 tests, verdes | ✅ ídem |
| E2E Playwright en flujo crítico | ⚠️ **inaplicable**: este repo no tiene harness E2E. Sustituido por el recorrido con la app del humano — que **no completó el rechazo** | ⚠️ ídem; el recorrido llegó al selector, no al corte |
| RLS en tabla nueva | ✅ **N/A**: no hay tabla nueva (sólo un valor de enum) | ✅ **N/A**: no hay tabla nueva (una columna en `orden`, que ya tiene régimen) |
| Migración con `down.sql` y `db:rollback` funciona | ⚠️ `down.sql` existe y está probado en estático; **el round-trip real NO se corrió** | ✅ round-trip real `up → down → down → up` citado en la bitácora |
| Sin secretos hardcodeados | ✅ (grep sobre el diff: 0) | ✅ |
| Webhooks con firma/idempotencia | ✅ **N/A**: ninguno nuevo (R44 lo afirma con test) | ✅ **N/A** (R32) |
| Controller sin queries ni negocio | ✅ `rechazarNovedad` sólo autentica, valida con zod y delega | ✅ las dos Server Actions igual |
| Service sin HTTP | ✅ `RechazoTiendaService`: 0 referencias a `Request`/`Response`/`headers`/`cookies` | ✅ |
| Repository sin lógica de negocio | ✅ el helper `transicionarDesdeDevuelta` es SQL + derivación de la fila ancla | ✅ |
| Interfaces en `lib/interfaces/`, por categoría | ✅ `IRechazoTiendaService`, `IGestionOrdenRepository` | ✅ `IRankingRepository`, `ICorteDiarioRepository`, … |
| Páginas protegidas validan permisos en servidor | ✅ el borde resuelve el actor por sesión y el service exige `adminTienda` **dueña** | ✅ sin cambio de permisos |
| Componentes `private/` reciben datos por props | ✅ | ✅ (las fechas del selector **bajan por props del Server Component**, R29) |
| Mutaciones por Server Actions, no fetch a API | ✅ | ✅ |
| Sin hardcode de país/moneda/cuenta | ✅ las únicas apariciones de `₡` están **en comentarios**; el aviso NO lleva cifra a propósito | ✅ |
| `./init.sh` en verde | ✅ corrido por el humano con el árbol quieto: EXIT 0, 1241 archivos / 16.332 tests / 359 s | ✅ ídem |
| `progress/review_<f>.md` con veredicto OK | este acta | este acta |
| Entrada en `progress/history.md` | ❌ pendiente (la estampa el leader al cerrar) | ❌ ídem |

### Sobre la base local (comprobado, no supuesto)

Consulté la base local en solo lectura: **las dos migraciones están aplicadas**
(`20260820180000_orden_fecha_reparto` y `20260820190000_orden_historial_origen_rechazo_tienda`), el
enum `orden_historial_origen_tipo` tiene **31 valores con `rechazo_tienda` dentro**, la columna
`orden.fecha_reparto` existe como `date` NULLABLE sin default, y el índice vivo es
`orden_mensajero_asignado_at_fecha_reparto_idx (mensajero_asignado_id, asignado_at, fecha_reparto)`.
**67 órdenes, 0 con `fecha_reparto`** (sin backfill, como R19 exige) y **0 filas de historial con
`rechazo_tienda`**: el rechazo manual **no se ha ejecutado nunca contra Postgres**.

⚠️ Dos cosas que conviene saber antes de probar el rollback:
1. `progress/impl_240.md` §0 afirma «LA MIGRACIÓN NO SE HA APLICADO A NINGUNA BASE». **Ya no es
   cierto** — la bitácora quedó rancia.
2. `_prisma_migrations` de la base local guarda una fila **fantasma**:
   `20260820160000_orden_historial_origen_rechazo_tienda`, la numeración vieja que se renumeró a
   `…190000`. Su carpeta no existe en el árbol y `prisma migrate status` **no se quejó** («Database
   schema is up to date!»). Es ruido del entorno local, no del repo, pero un `db:rollback` a ciegas
   podría apuntar a algo que no está.

---

## 10 · HALLAZGOS

### 🔴 BLOQUEANTE-1 (240) · R24 no tiene ninguna evidencia, y la que lo cubría nunca se ejecutó

**R24:** «El sistema NO DEBE re-anclar, borrar ni modificar el registro de anclaje de la devolución
de esa orden.»

`progress/impl_240.md` lo declara sin disimulo: `| R24 | — | NO CUBIERTO AQUÍ. Es T8.3 (contra
Postgres, con la app corrida) |`. **T8.3 no se hizo** (§4.5 de la misma bitácora: «R24 sigue sin
comprobarse contra Postgres»), y el recorrido del humano tampoco llegó ahí —el rechazo **no se
ejecutó**, y la base local tiene **0 filas** con `rechazo_tienda`—. R24 no tiene ni test ni
medición: es el único de los 93 requisitos de las dos fichas que se queda sin nada.

**Lo medí antes de escribirlo, para no rechazar por un formalismo.** Mutación REV-7: se añade a
`rechazarDesdeDevuelta`, dentro de la transacción, un
`tx.ordenHistorialEstado.deleteMany({ where: { ordenId, origenTipo: "anclaje_devolucion" } })` — es
decir, el rechazo **borra el ancla**.

```
SHA ANTES:   a7d0bf4e484c98244cbe30ce4c704a877cf96cee0f7aee5d479f39df6a466733
SHA DESPUES: a7d0bf4e484c98244cbe30ce4c704a877cf96cee0f7aee5d479f39df6a466733
```

Resultado: `Tests 9 failed` en `gestion-orden-rechazar.test.ts`. **Pero muere por accidente:** el
doble de ese archivo expone `ordenHistorialEstado: { createMany }` y nada más, así que el mutante
revienta con un `TypeError`, no con una aserción. **Ningún caso nombra R24 y ninguno afirma la
propiedad.** El día que alguien añada `deleteMany: vi.fn()` a ese doble por cualquier otra razón
—cosa que pasa— R24 se queda sin red y **nada lo dirá**.

**Qué falta para cumplirlo** (barato, y sin necesidad de Postgres): un caso nombrado en
`tests/unit/repositories/gestion-orden-rechazar.test.ts` —«R24: el rechazo NO toca el registro de
anclaje de la devolución»— con el doble ampliado a
`{ createMany, update, updateMany, delete, deleteMany }` **todos espiados**, afirmando que los
cuatro de escritura **no se llaman** y que el único `createMany` lleva **una sola fila**, de familia
`rechazo_tienda`. Con eso la muerte pasa a ser por aserción y R24 entra en el mapa.
*(Mejor aún, y cierra T8.3: ejecutar un rechazo real contra la base local —la migración ya está
aplicada— y comprobar que la fila `anclaje_devolucion` conserva su `id` y su `created_at`.)*

### 🔴 BLOQUEANTE-2 (240) · `tasks.md` sin marcar, y con tareas realmente sin hacer

`specs/240-rechazo-manual-tienda/tasks.md`: **0 de 37 tareas marcadas `[x]`**. Y no es sólo
contabilidad: hay cinco que **no están hechas** y que `tasks.md` no declara fuera de alcance:

- **T4.3** (el badge «La tienda» en el cierre) — no hecha, con un argumento **bueno** en las dos
  bitácoras: el archivo que el spec cita (`RepartoAyudaResueltaPorLaTienda.test.tsx`) es del portal
  del mensajero, no del cierre. Verificado por mí: su cabecera dice «FEATURE 237 … LA ORDEN SALE DEL
  PORTAL DEL MENSAJERO» y monta `RepartoModule`. **El argumento se acepta**, pero la salida correcta
  es **corregir `tasks.md`** dejando la tarea cerrada con su motivo, no dejarla en blanco: mañana
  nadie sabrá si fue decisión u olvido.
- **T8.2** (la mitad del mensajero y de la bodega del recorrido) — no hecha.
- **T8.3** (contra Postgres) — no hecha. **Es la que deja R24 sin evidencia** (BLOQUEANTE-1).
- **T9.1 / T9.2** (anotar `progress/auditoria_ayuda_tienda.md` y
  `progress/design_pila_ayuda_tienda.md`) — no hechas: ninguno de los dos aparece en el diff.
- **T8.1** quedó **a medias**: el recorrido llegó a la ventana y **no ejecutó el rechazo**, cosa que
  `recorrido_240_246.md` dice sin disimular.

### ⚠️ menor-1 (240) · La guardia anti-maqueta mide el `import`, no la llamada

Detallado en §2, con las dos mutaciones y sus sha. Se puede replantar la maqueta dejando el `import`
en pie y quitando la invocación, y **las dos guardias siguen verdes**; incluso la variante ingenua
(import huérfano) pasa, porque `pnpm run lint` es `eslint` **sin `--max-warnings=0`** y el
`no-unused-vars` sale como *warning*. Quien la mata es el test de componente — justo la red que D3
declaró insuficiente. **Remedio:** exigir en el frente 2 que el símbolo aparezca además como
invocación en alguno de los archivos que lo importan, con su autocomprobación contra un fuente
sintético que importe-y-no-llame. *(Observación de repo, aparte: `--max-warnings=0` en el script de
lint convertiría este sabor concreto en rojo del gate para toda la casa.)*

### ⚠️ menor-2 (240) · El round-trip real de la migración nunca se corrió

El `down.sql` existe, recrea el tipo con los 30 valores y falla ruidosamente si quedan filas con la
familia nueva — probado **en estático**. Pero `pnpm run db:rollback` **no se ejecutó ni una vez**, y
CHECKPOINTS pide que el script *funcione*. La 246 sí lo hizo (`up → down → down → up`, citado).
Barato de cerrar: la migración ya está aplicada en local.

### ⚠️ menor-3 (240) · La bitácora quedó rancia

`progress/impl_240.md` §0 afirma «⚠️ LA MIGRACIÓN NO SE HA APLICADO A NINGUNA BASE», y
`impl_240_frontend.md` §7.5 lo repite. Comprobado contra la base local: **sí está aplicada**, con el
enum en 31 valores. Dos frases que hoy son falsas en el sitio donde alguien las creerá.

### ⚠️ menor-4 (entorno) · Fila fantasma en `_prisma_migrations` local

`20260820160000_orden_historial_origen_rechazo_tienda` figura como aplicada y su carpeta no existe
(se renumeró a `…190000`). `prisma migrate status` dijo «Database schema is up to date!» sin
mencionarlo. No es del árbol, pero conviene saberlo **antes** de probar el rollback de menor-2.

### ⚠️ menor-5 (heredado de la 237) · Un comentario que dice el dinero al revés

`app/(app)/novedades/_components/NovedadAcciones.tsx:78` — «un rechazo cobra a la tienda hasta
₡1.000». Los ₡1.000 son **ingreso de bodega** (`cobroRechazado`); lo que la tienda paga es el flete
de devolución. Es la confusión exacta que D10 de la 240 corrigió en sus propios textos. La línea es
**anterior** a este commit (`git show 19f74829` la trae igual), pero vive en un archivo que la 240
edita, así que aquí es donde se ve.

### ⚠️ menor-6 (240) · Un nombre que ya no describe lo que lleva

`desdeAyudaTienda` (en `CierreGestionPendienteRow` y en los DTO) significa ahora «lo registró la
tienda **por cualquiera de sus dos vías**». El rename a `registradaPorLaTienda` está **declarado
como deuda junto al campo** y diferido con razón (≈40 archivos, varios en la superficie de la 246).
Bien manejado; queda como seguimiento.

### ⚠️ menor-7 (246) · Las fechas del selector son opcionales en los contenedores

Obligatorias en el selector y en los dos modales (el typecheck lo demostró: 5 rojos = 5 montajes),
pero opcionales con defecto `{hoy:"",manana:""}` en `OrdenesListado` y `RecepcionSateliteModule`. Si
una página se olvidara, **degrada en silencio**. El autor lo declara, argumenta que nada miente y
ofrece el cambio; las dos páginas **sí** las pasan hoy y el recorrido lo vio con la fecha puesta.

### ⚠️ menor-8 (246) · R44 está medido por FORMA, no por coste

Los cuatro `EXPLAIN` están citados con su salida y **la decisión se invirtió gracias a ellos**. Pero
producción tiene 141 órdenes vivas y la local 67: la forma se forzó con `enable_seqscan = off`. Está
escrito en tres sitios («⏳ re-medir con volumen»), igual que el coste que ningún índice arregla (el
`Index Cond` se reduce a `mensajero_asignado_id IS NOT NULL` y se pierde el acotado por rango). No es
un hallazgo contra la ficha: es lo que hay que volver a mirar.

### ⚠️ menor-9 (proceso) · Dos `in_progress` de la misma zona compartiendo archivos

`CLAUDE.md` regla 1 admite 2 features `in_progress` por zona **«siempre sin conflicto de archivos
entre ellas»**. Estas dos compartieron cinco archivos sobre el mismo árbol. Se hizo con cuidado
—cada edición lleva su ficha citada, ninguna reescribió un archivo entero, y el resultado es
verificablemente limpio— pero es una excepción a la regla y merece quedar escrita.

### ⚠️ menor-10 (246) · `tasks.md` sin marcar (contabilidad)

1 de 47. A diferencia de la 240, aquí marcarlas sería **veraz**: lo pendiente es T6.7 (**N/A**: D10
se firmó «el tablero NO sigue al ranking»), T7.7 (el recorrido, hecho a medias por el humano y
declarado) y T8.3 (el cierre de ficha, que estampa el leader). **Hay que marcarlas antes de pasar a
`done`**, con esas tres anotadas por lo que son.

### 🔎 Observación, sin categoría · el orden `not_found` antes que `forbidden`

`RechazoTiendaService.rechazar` devuelve `not_found` **antes** de comprobar la autorización, así que
una tienda ajena puede distinguir «esa orden no existe» de «existe y no es tuya». No lo cuento como
hallazgo: el `ordenId` es un UUID (no enumerable) y es **el patrón establecido** del repo —
`ReprogramacionTiendaService` hace exactamente lo mismo—. Se anota para que sea decisión el día que
alguien lo mire.

---

## 11 · Mutaciones de esta revisión — resumen

Todas una a una, con el árbol restaurado y re-verificado (`SHA DESPUES == SHA ANTES` en las siete),
y `git diff HEAD` **vacío** al terminar. Ninguna se corrió en paralelo con otra.

| # | Qué se rompe | Archivo | ¿Muerta? | Quién la mata |
| --- | --- | --- | --- | --- |
| REV-1 | 💰 el rechazo manual sin gestión sintética | `GestionOrdenRepository.ts` | **sí**, 6 casos | sólo el test de REPOSITORIO |
| REV-2 | 💰 la guarda de estado del `UPDATE` (carrera con el cron) | `GestionOrdenRepository.ts` | **sí**, 4 casos | repositorio (rechazo + reprogramación) |
| REV-3 | el ancla del corte pasa a ser «hoy» | `CorteDiarioService.ts` | **sí**, 9 casos | servicio de corte y selección |
| REV-4 | 💰 la rama de respaldo del denominador (T7.4) | `RankingRepository.ts` | **sí**, 3 casos | **sólo** el repositorio; los 65 de servicio siguen verdes |
| REV-5 | una escritura de `asignado_at` dentro de la migración autorizada | `…_orden_fecha_reparto/migration.sql` | **sí**, 1 caso | la guardia de dinero, por el caso NUEVO de la 246 |
| REV-6 / 6b | 🔴 replantar la maqueta dejando el `import` | `RechazarNovedadModal.tsx` | **NO por las guardias** | sólo el test de componente |
| REV-7 | 🔴 el rechazo borra el ancla de la devolución (R24) | `GestionOrdenRepository.ts` | sí, pero **por `TypeError`**, no por aserción | ninguna aserción la nombra |

Y una octava, de confirmación del censo de la invariante:

| REV-8 | quitar `fechaReparto: null` de uno de los seis sitios de limpieza | `DevolucionSlaRepository.ts` | **sí**, 3 casos | la guardia `fecha-reparto-acompana-asignado-at` (2) + el repositorio (1) |

---

## VEREDICTO FINAL

### 246 — asignación por día · **OK**

Los cuatro puntos de dinero se sostienen bajo mutación: el ancla del corte, la rama de respaldo del
denominador (con la suite de servicio verde, como estaba avisado), el censo de la invariante
`fecha_reparto ↔ asignado_at`, y la guardia de `asignado_at`, que quedó **más estricta** y no menos.
El índice conserva el prefijo y el `down.sql` lo repone con su nombre exacto. La decisión del índice
se **invirtió con medición** y el error intermedio se dejó escrito. Los 46 requisitos tienen dueño;
el único sin test (R44) tiene cuatro `EXPLAIN` citados y la forma atornillada por cuatro casos, con
la re-medición declarada.

**Lo que queda fuera, y con qué remite:**
1. **Marcar `tasks.md`** (menor-10): T6.7 como **N/A** por la firma de D10, T7.7 como **parcial** con
   lo que el recorrido no vio (el corte nocturno), T8.3 al leader. → **antes de pasar a `done`**.
2. **Re-medir los planes con volumen** (menor-8). → seguimiento, ya escrito en tres sitios.
3. **Decidir si las fechas del selector pasan a obligatorias en los contenedores** (menor-7). → el
   autor lo ofrece; es mecánico.
4. **Entrada en `progress/history.md`.** → el leader.

### 240 — rechazo manual de la tienda · **RECHAZADO**

No por la sustancia, que es buena: la paridad con el cron está escrita donde debe, la carrera no
puede duplicar el cobro, la maqueta murió de verdad y la guardia nueva sí cierra los cuatro frentes
que declara. Se rechaza por **dos bloqueantes acotados**:

- **BLOQUEANTE-1 · R24 sin evidencia.** Ni test ni medición: T8.3 nunca se corrió y el rechazo nunca
  se ejecutó contra Postgres. La mutación muere por `TypeError`, no por aserción.
  **Remite:** un caso nombrado en `gestion-orden-rechazar.test.ts` con el doble de
  `ordenHistorialEstado` ampliado a los cinco métodos y espiados los cuatro de escritura. Diez líneas.
- **BLOQUEANTE-2 · `tasks.md` en blanco con tareas realmente sin hacer** (T4.3, T8.2, T8.3, T9.1,
  T9.2, y T8.1 a medias).
  **Remite:** cerrar T8.3 (que además cierra BLOQUEANTE-1), hacer T9.1/T9.2, y **anotar T4.3 como
  descartada con su motivo** —el argumento es correcto y está escrito dos veces, pero tiene que vivir
  en `tasks.md`, no sólo en las bitácoras—. T8.2 es un recorrido del humano.

Al implementer, no al reviewer. Cerrado eso, **el resto de la 240 pasa**: no encontré ningún otro
requisito sin test, ninguna cita falsa, ningún borde pisado y ningún secreto ni hardcode.

**Y una recomendación que no bloquea a ninguna de las dos:** cerrar menor-1 (la guardia mide el
`import`, no la llamada). Es la mitad que le falta a la única red que D3 puso para que el problema
no vuelva.
