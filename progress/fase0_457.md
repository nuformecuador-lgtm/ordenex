# Ficha 457 — Fase 0: caracterizar lo que NO puede cambiar

**SHA de partida:** `c9c4b005` (`origin/dev` con la 461 mergeada, PR #826). Rama `feature/457-backend`.
**Base:** clon `ordenex_457` (`CREATE DATABASE ordenex_457 TEMPLATE ordenex`), `prisma migrate status` →
«Database schema is up to date!» (última aplicada `20260926120500_wallet_461_fechas_cr_pagos`).
**Fecha:** 2026-09-25.

## T0.1 — La fotografía y la invariante, verdes sobre el SHA de partida

```
pnpm exec vitest run tests/integration/db/caja-caracterizacion-459.test.ts tests/integration/db/caja-invariante-tiendas.test.ts
 Test Files  2 passed (2)
      Tests  26 passed (26)
VITEST_EXIT=0
```

`skipped = 0` (los dos archivos van envueltos en `HAY_BASE_DE_DATOS` y la base del clon resuelve).

### Los literales de HOY (los que esta ficha promete no mover; `caja-caracterizacion-459.test.ts`)

Todas son DIFERENCIAS «después − antes» sobre el libro entero, en REPEATABLE READ.

| Cifra | Literal |
| --- | --- |
| «Entró» (`entradas`) | 87 517,25 |
| «Salió» (`salidas`) | 161 845,77 |
| Cifra principal (`enCaja`) | −74 328,52 |
| Egresos propios | 153 845,77 |
| Ganancia (bloque 461) | −87 374,34 |
| «De las tiendas» (`deTerceros`) | 13 045,82 (= Σ saldos 7 530,70 + 5 515,12, sin excepción) |
| Capital | 0,00 |
| «De Ordenex» | −87 374,34 |
| Tienda A: saldo / aFavor / cargos / pagado | 7 530,70 / 19 900,00 / 7 369,30 / 5 000,00 |
| Tienda B: saldo / aFavor / cargos / pagado | 5 515,12 / 16 617,00 / 8 101,88 / 3 000,00 |
| Mensajero: devengado / pagado / cuenta por pagar | 10 500,00 / 8 664,00 / 1 836,00 |
| Composición: ingreso_flete / devolución / comisión / IVA flete / IVA devolución / IVA comisión / ajuste | 8 000,00 / 2 500,00 / 978,47 / 1 040,00 / 325,00 / 127,21 / 51 000,25 |
| Composición egresos: pago mensajero / ajuste / reverso cobro / otros / total | 9 500,00 / 500,10 / 0,00 / 0,00 / 153 845,77 |
| Desglose egresos: fijo / variable / sueldo / indemnización / total | 80 000,00 / 12 345,67 / 45 000,00 / 6 500,00 / 143 845,67 |
| Filas por libro (escenario) | caja 22 (sin el cobro) + 1 (`cobro_tienda`) = 23 · tienda A 9 · tienda B 9 · mensajero 7 |

Invariante (`caja-invariante-tiendas.test.ts`, 11 pasos): filas nuevas en caja por paso
`[23, 1, 1, 1, 1, 1, 0, 1, 1, 1, 1]`; R7 y R8 al céntimo tras cada paso salvo la foto legada (rota a
propósito por 1 000,00 y luego por 300,00).

## T0.2 — Mutaciones de control (a)–(e) de `design.md` §13, con autocomprobación

Protocolo por mutación (script `mutaciones-control.sh`, ejecutado desde el worktree): `sed` de UNA
línea → `git diff --numstat` debe ser exactamente `1 1` → `vitest run` SOLO sobre los archivos
nombrados → se anotan los `FAIL` con nombre de caso y la línea `Tests` → `git checkout -- <archivo>`
→ `git diff --stat` vacío. Las cinco cumplieron `1 1` y dejaron el árbol limpio.

| Mut. | Archivo · línea mutada | Tests corridos | Resultado |
| --- | --- | --- | --- |
| (a) | `LiquidacionService.ts` — `await this.caja.emitirEgresoDePago(tx, {` → `await Promise.resolve({` | fotografía + invariante | **ROJO** · `Tests 8 failed \| 18 passed (26)` · casos: «R93: Salió», «R95/R96: filas de la CAJA», «Entró, cifra principal, De las tiendas…», «R1/R2/R3 (461)», «R4 (461)», «dos siembras…», invariante «R8 (HD2)» y «anti-vacuidad» |
| (b) | `caja-tesoreria.ts` — `LIQUIDEZ.ingreso_flete: "cargo_a_tienda"` → `"efectivo"` | fotografía + invariante | **ROJO** · `Tests 3 failed \| 23 passed (26)` · casos: «Entró, cifra principal…», «R4 (461)», invariante «R8 (HD2)» |
| (c) | `desglose-tienda.ts` — `CUBETA.cobro_manual: "cargos"` → `"aFavor"` | fotografía + invariante | **ROJO** · `Tests 1 failed \| 25 passed (26)` · caso: «R92: saldo y desglose de la tienda B» |
| (d) | `CobroTiendaService.ts` — `await this.caja.emitirCargoDeCobro(tx, {` → `await Promise.resolve({` | fotografía + invariante | **ROJO** · `Tests 8 failed \| 18 passed (26)` · casos: «R1/R2/R3», «R27», «la ganancia sube EXACTAMENTE el cobro», «Entró, cifra principal…», «dos siembras…», invariante «R23–R26», «R8 (HD2)», «anti-vacuidad» |
| (e) | `PagoPorCuentaTiendaService.ts:179` — `await this.candado.bloquearBeneficiario(tx, {…})` → `void input.tiendaId;` | fotografía + invariante + `pago-por-cuenta-tienda-concurrencia` | **SUPERVIVIENTE** · `Tests 29 passed (29)` |

**(e) explicada, no escondida.** Es el mutante que la 459 ya midió como EQUIVALENTE hoy
(`pago-por-cuenta-tienda-concurrencia.test.ts`, cabecera): el pago de un gasto escribe ANTES de leer
nada y sus `INSERT` con FK a `usuario` toman `FOR KEY SHARE` sobre la fila de la tienda, que choca con
el `FOR UPDATE` del otro lado; su candado es defensa en profundidad. `design.md` §13 lo preveía: «si
ninguno la mata, la concurrencia de esta ficha la cubre». En la 457 el candado SÍ decide: el abono LEE
el saldo bajo el candado antes de escribir (R14/R15/R16), así que quitarlo (mutación 5 de la ficha) lo
tiene que poner rojo `abono-tienda-457-concurrencia.test.ts` (abono ∥ abono sobre la misma deuda).
Se mide en T9.1.

Logs: `mut_a.log` … `mut_e.log` en el scratchpad de la sesión; resumen copiado arriba.

## T0.4 — M3 y M4 medidos en el clon LOCAL (ANTES de escribir la migración)

`base: ordenex_457 @ localhost` (script de solo lectura con las consultas literales de `design.md` §14).

| Catálogo | n | Últimos |
| --- | --- | --- |
| `wallet_movimiento_categoria` | **23** | …, `ingreso_aporte_capital`, `egreso_reverso_aporte_capital`, `ingreso_cobro_tienda`, `egreso_reverso_cobro_tienda` |
| `wallet_tienda_movimiento_categoria` | **14** | …, `cobro_manual`, `pago_por_cuenta`, `pago_por_cuenta_anulado`, `cobro_tienda_anulado` |
| `wallet_origen_tipo` | **13** | …, `pago_por_cuenta_tienda`, `aporte_capital`, `cobro_manual_reclasificado`, `cobro_tienda`, `cobro_tienda_completado` |
| `historial_accion_tipo` | **61** | …, `aporte_capital_anulado`, `cobro_tienda_anulado`, `wallet_movimiento_manual_anulado` |
| `historial_accion_entidad` | **23** | …, `wallet_tienda_movimiento`, `pago_por_cuenta_tienda`, `aporte_capital` |

**Coinciden exactamente con `design.md` §1.1 (23 / 14 / 13 / 61 / 23) y con las listas de la 461.**

M4 (los dos CHECK, leídos de `pg_constraint`): caja = 12 ingresos (`ingreso_flete`, `ingreso_flete_devolucion`,
`ingreso_comision_cod`, `ingreso_iva_flete`, `ingreso_iva_flete_devolucion`, `ingreso_iva_comision_cod`,
`ingreso_ajuste`, `ingreso_cod_recaudado`, `ingreso_reverso_pago_tienda`, `ingreso_reverso_pago_por_cuenta_tienda`,
`ingreso_aporte_capital`, `ingreso_cobro_tienda`) + 11 egresos (`egreso_pago_tienda`, `egreso_pago_mensajero`,
`egreso_gasto`, `egreso_sueldo`, `egreso_ajuste`, `egreso_gasto_fijo`, `egreso_gasto_variable`,
`egreso_indemnizacion`, `egreso_pago_por_cuenta_tienda`, `egreso_reverso_aporte_capital`,
`egreso_reverso_cobro_tienda`); tienda = 4 créditos (`cod_recaudado`, `ajuste_credito`, `pago_por_cuenta_anulado`,
`cobro_tienda_anulado`) + 10 débitos (`flete`, `flete_devolucion`, `comision_cod`, `iva_flete`,
`iva_flete_devolucion`, `iva_comision_cod`, `pago_tienda`, `ajuste_debito`, `cobro_manual`, `pago_por_cuenta`).
**= las listas de partida de `design.md` §3.3.** Las migraciones de la Fase 1 parten de ESTAS.

M7: `to_regclass('public.abono_tienda')` = `null` (no existe); `liquidacion_pago` con `tienda_id` = 0.

T0.3 (producción, MCP de Supabase) la corre el LEADER: `progress/contraste_457.md`.

## T9.1 — anexo: la fase 0 repetida sobre el árbol final (2026-09-26)

**Árbol:** `6f47a5ba` (`feature/457-backend` con el recorrido y la revisión mergeados y los arreglos del
cierre: m1–m5, m7, F1, asistente, descargas). El código de producción de ese SHA es el final: lo que se
commitea después son documentos y el log del gate. **Base:** clon `ordenex_457c` (`TEMPLATE ordenex` +
`prisma migrate deploy`, las dos migraciones de la ficha aplicadas).

### La fotografía y la invariante, verdes SIN tocar literales

```
pnpm exec vitest run tests/integration/db/caja-caracterizacion-459.test.ts tests/integration/db/caja-invariante-tiendas.test.ts
 Test Files  2 passed (2)
      Tests  27 passed (27)
```

El diff de `tests/integration/db/caja-caracterizacion-459.test.ts` entre `c9c4b005` (partida) y
`6f47a5ba` está vacío: ni un literal de la fotografía cambió en toda la ficha. La invariante SÍ cambió
(+3 pasos de la 457 y la renumeración del paso del saldo inicial; 26 → 27 casos con la fotografía),
como manda `design.md` §13.

### Las 14 mutaciones de `design.md` §13, todas ROJAS sobre el árbol final

Arnés `mut.py` + `todas.py` (scratchpad de la sesión, fuera del árbol) con AUTOCOMPROBACIÓN por mutación:
el archivo sin cambios antes de mutar → el texto a mutar aparece EXACTAMENTE una vez → `--numstat` no
vacío → vitest SOLO sobre los tests nombrados, con línea `Tests` y ejecutados > 0 → `checkout` del
archivo → diff vacío (si no, aborta). Una sola corrida, las 14 seguidas, árbol limpio al final. Ningún
gate corría a la vez.

| Mut. | Dónde · cambio | numstat | Resultado | Primeros casos en rojo | R |
| --- | --- | --- | --- | --- | --- |
| 1 | `AbonoTiendaService.registrar`: `await this.caja.emitirIngresoDeAbono(tx, {` → `await Promise.resolve({` | `1 1` | **ROJA** · `18 failed \| 40 passed (58)` | «CONTRAPRUEBA (mutación 1)», «R1/R5/R17/…», «R19/R39», «R8 (HD2)» de la invariante | R17/R23 |
| 2 | `caja-tesoreria.ts` `NATURALEZA.ingreso_abono_tienda` `"terceros"` → `"propio"` | `1 1` | **ROJA** · `11 failed \| 35 passed (46)` | «R19/R39», «R23–R26…», «R8 (HD2)», guardia «R9/R90», `caja-derivacion-457` | R19/R24 |
| 3 | `caja-tesoreria.ts` `LIQUIDEZ.ingreso_abono_tienda` `"efectivo"` → `"cargo_a_tienda"` | `1 1` | **ROJA** · `11 failed \| 35 passed (46)` | guardia «(4) literal del contrato», «R19/R39», «R8 (HD2)», «Entró y la cifra suben EXACTAMENTE M» | R19/R24 |
| 4 | `desglose-tienda.ts` `CUBETA.abono_tienda` `"aFavor"` → `"cargos"` | `1 1` | **ROJA** · `1 failed \| 24 passed (25)` | «R8: la cubeta CONCUERDA con el tipo…» | R49 |
| 5 | `AbonoTiendaService.registrar`: quitar `bloquearBeneficiario` → `void input.tiendaId;` | `1 1` | **ROJA** · `4 failed \| 31 passed (35)` | concurrencia «R16 (mutación 5)… excede», «R16: … EXACTAMENTE lo que queda», unitario «R16/R17», «R16: el saldo que DECIDE…» | R16 |
| 6 | `reglaDelDinero`: `monto.gt(deuda)` → `monto.gte(deuda)` | `1 1` | **ROJA** · `4 failed \| 35 passed (39)` | «R15/R66 (mutación 6)», «R15/R66: por encima…», «R10/R11», «R25 (dos envíos A LA VEZ)» | R15 |
| 7 | `AbonoTiendaService.anular`: `monto: abono.monto` → `(input as { monto?: string }).monto ?? abono.monto` (la petición trae `monto: "1.00"`) | `1 1` | **ROJA** · `1 failed \| 7 passed (8)` | «R31/R34/R38 (mutaciones 7 y 8)» | R34 |
| 8 | `AbonoTiendaService.anular`: sin el débito (`crearMovimientos` → `Promise.resolve`) | `1 1` | **ROJA** · `8 failed \| 21 passed (29)` | «R31–R33/R36–R39», «R65 (T8.3)», «R23–R26…», «R8 (HD2)» | R31/R23 |
| 9 | `formDataAbono`: se quita `fd.set("claveIdempotencia", clave);` | `0 1` | **ROJA** · `3 failed \| 67 passed (70)` | «R56 (mutación 9)», «R56: la fecha viaja SIEMPRE…», «registra con el id de la tienda FIJA…» | R56 |
| 10 | `WalletService.tipoDeDocumentoOriginal`: rama del abono → `if (false) {` | `1 1` | **ROJA** · `3 failed \| 48 passed (51)` | integración «R41», «UNA consulta por tipo…», «solo los ORIGINALES llevan documento…» | R41 |
| 11 | `CATEGORIA_MI_WALLET_LABEL.abono_tienda` → «La tienda le paga a Ordenex» (tablas, descargas y diccionario) | `1 1` | **ROJA** · `9 failed \| 48 passed (57)` | «R44: es DISTINTA…», «descarga: las mismas palabras…», «tabla: «Le pagaste a Ordenex»…», «filtro por concepto…» | R47/R53 |
| 12 | `CobroTiendaService.ts`: +`export const MUTANTE_457 = { categoria: "abono_tienda" };` (segundo productor) | `1 0` | **ROJA** · `2 failed \| 8 passed (10)` | «CONTRAPRUEBA (mutación 12)», «el censo de productores es EXACTAMENTE el servicio» | R65 |
| 13 | `PagoTiendaAcciones`: `const tiendaDebe = signo === "negativo";` → `false` | `1 1` | **ROJA** · `3 failed \| 39 passed (42)` (con `pago-tienda-acciones` y `wallet-tiendas-pago`, verdes) | «con saldo negativo, el desglose ofrece…», «concepto… y tienda… deshabilitados», «tras registrar, se releen…» | R59 |
| 14 | `…_457_tablas_y_checks/down.sql`: `RAISE EXCEPTION 'rollback 457:` → `RAISE NOTICE` | `1 1` | **ROJA** · `1 failed \| 5 passed (6)` | «(d) R75: el down de la migración 2 ABORTA con UNA fila…» | R75 |

Tests corridos por mutación (solo esos): 1 → `abono-tienda-457`, `abono-tienda-alcance.guardia`,
`abono-tienda-service`, `caja-invariante-tiendas`; 2 y 3 → `abono-tienda-457`, `caja-clasificacion-459.guardia`,
`caja-derivacion-457`, `caja-invariante-tiendas`; 4 → `desglose-tienda`; 5 → `abono-tienda-457-concurrencia`,
`abono-tienda-service`; 6 → `abono-tienda-457`, `abono-tienda-service`; 7 → `abono-tienda-anulacion`; 8 →
`abono-tienda-457`, `abono-tienda-anulacion`, `caja-invariante-tiendas`; 9 → `wallet-registrar-movimiento-dialog`;
10 → `abono-tienda-457`, `wallet-service`; 11 → `mi-wallet-labels`, `nombres-wallet-461.guardia`,
`DesgloseTiendaAbono457`; 12 → `abono-tienda-alcance.guardia`; 13 → `PagoTiendaAccionesAbono457`,
`pago-tienda-acciones`, `wallet-tiendas-pago`; 14 → `abono-tienda-457-migration`.

### Las mismas 14 en las dos sesiones anteriores (para comparar)

| Mut. | Backend (`progress/impl_457.md` §7, sobre `a7bd1186`) | Frontend (§11.5, sobre `fd471b7e`) | Árbol final (`6f47a5ba`) |
| --- | --- | --- | --- |
| 1 | ROJA · 18 failed \| 38 passed (56) | — | ROJA · 18 \| 40 (58) |
| 2 | ROJA · 11 \| 35 (46) | — | ROJA · 11 \| 35 (46) |
| 3 | ROJA · 11 \| 35 (46) | — | ROJA · 11 \| 35 (46) |
| 4 | ROJA · 1 \| 51 (52) | — | ROJA · 1 \| 24 (25) |
| 5 | ROJA · 4 \| 28 (32) | — | ROJA · 4 \| 31 (35) |
| 6 | ROJA · 4 \| 34 (38) | — | ROJA · 4 \| 35 (39) |
| 7 | ROJA · 1 \| 28 (29) | — | ROJA · 1 \| 7 (8) |
| 8 | ROJA · 8 \| 21 (29) | — | ROJA · 8 \| 21 (29) |
| 9 | (UI) | ROJA · 3 \| 67 (70) | ROJA · 3 \| 67 (70) |
| 10 | ROJA · 3 \| 48 (51) | — | ROJA · 3 \| 48 (51) |
| 11 | ROJA · 6 \| 42 (48) (diccionario y guardia) | ROJA · 3 \| 5 (8) (tablas, descargas, filtro) | ROJA · 9 \| 48 (57) (las dos a la vez) |
| 12 | ROJA · 2 \| 7 (9) | — | ROJA · 2 \| 8 (10) |
| 13 | (UI) | ROJA · 3 \| 39 (42) | ROJA · 3 \| 39 (42) |
| 14 | ROJA · 1 \| 5 (6) | — | ROJA · 1 \| 5 (6) |

Los totales cambian solo porque los conjuntos de tests nombrados no son idénticos entre sesiones (la 4
y la 7 corrieron aquí sobre menos archivos; la 5 suma los casos m2/m3 de la concurrencia); los casos que
caen son los mismos.

### Las mutaciones del cierre (hallazgos de la revisión y del recorrido), también rojas

| Id | Dónde · cambio | numstat | Resultado | Caso que la mata |
| --- | --- | --- | --- | --- |
| H (m1) | `lib/analytics/metrics.ts`: fuera `"abono_tienda"` y `"abono_tienda_anulado"` de `cuenta_por_pagar_tienda` | `0 2` | **ROJA** · `2 failed \| 2259 passed` (sobre `tests/unit/analytics`) | `metrics-cuenta-por-pagar-tienda-457.test.ts` (los dos casos) |
| A (m2) | `AbonoTiendaService.anular`: quitar `bloquearBeneficiario` | `1 1` | **ROJA** · `1 failed \| 18 passed (19)` (integración + concurrencia, SIN el unitario) | concurrencia «R38 (m2)…: la ANULACIÓN toma el candado…» |
| m3 | `registrar`: `this.saldoDe(input.tiendaId, tx)` → `this.saldoDe(input.tiendaId)` | `1 1` | **ROJA** · `2 failed \| 33 passed (35)` | concurrencia «R16 (m3)…: con UNA sola conexión en el pool…» y unitario «m3: la lectura del saldo que DECIDE…» |
| m3b | `WalletTiendaMovimientoRepository.agregarSaldoPorTienda`: `(cliente ?? this.prisma)` → `(this.prisma)` | `1 1` | **ROJA** · `1 failed \| 7 passed (8)` | concurrencia «R16 (m3)…» |
| m5 | `CajaBackfillTesoreriaService.dePagosATienda` lee `abonoTienda` en vez de `liquidacionPago` | `2 2` | **ROJA** · `1 failed \| 11 passed (12)`; cae en la aserción de los PENDIENTES (el pago sale como `egreso_pago_tienda` pendiente) | integración «R74» |
| m7 | diálogo: `ya_registrado` con `TEXTO_ABONO.registrado` (sin el importe) | `1 1` | **ROJA** · `1 failed \| 69 passed (70)` | «R25/R57 + m7: `ya_registrado`… dice el importe que QUEDÓ» |
| asistente | `MAX_TOKENS_DEFAULT = 2048` → `1024` | `1 1` | **ROJA** · `1 failed \| 10 passed (11)` | «el techo por defecto es 2048…» |
| descargas | `nombreArchivoDescarga` con la fecha LOCAL (`getFullYear/getMonth/getDate`) | `1 1` | **ROJA** · `2 failed \| 16 passed (18)` | «a las 23:00 de CR…», «construirDescarga usa el mismo nombre…» |
