# Ficha 457 — Fase 0: caracterizar lo que NO puede cambiar

**SHA de partida:** `c9c4b005` (`origin/dev` con la 461 mergeada, PR #826). Rama `feature/457-backend`.
**Base:** clon `ordenex_457` (`CREATE DATABASE ordenex_457 TEMPLATE ordenex`), `prisma migrate status` →
«Database schema is up to date!» (última aplicada `20260926120500_wallet_461_fechas_cr_pagos`).
**Fecha:** 2026-09-25.

## T0.1 — La fotografía y la invariante, verdes sobre el SHA de partida

```
pnpm exec vitest run tests/integration/db/caja-caracterizacion-459.test.ts tests/integration/db/caja-invariante-tiendas.test.ts
 Test Files  2 passed (2)
      Tests  26 passed (26)
VITEST_EXIT=0
```

`skipped = 0` (los dos archivos van envueltos en `HAY_BASE_DE_DATOS` y la base del clon resuelve).

### Los literales de HOY (los que esta ficha promete no mover; `caja-caracterizacion-459.test.ts`)

Todas son DIFERENCIAS «después − antes» sobre el libro entero, en REPEATABLE READ.

| Cifra | Literal |
| --- | --- |
| «Entró» (`entradas`) | 87 517,25 |
| «Salió» (`salidas`) | 161 845,77 |
| Cifra principal (`enCaja`) | −74 328,52 |
| Egresos propios | 153 845,77 |
| Ganancia (bloque 461) | −87 374,34 |
| «De las tiendas» (`deTerceros`) | 13 045,82 (= Σ saldos 7 530,70 + 5 515,12, sin excepción) |
| Capital | 0,00 |
| «De Ordenex» | −87 374,34 |
| Tienda A: saldo / aFavor / cargos / pagado | 7 530,70 / 19 900,00 / 7 369,30 / 5 000,00 |
| Tienda B: saldo / aFavor / cargos / pagado | 5 515,12 / 16 617,00 / 8 101,88 / 3 000,00 |
| Mensajero: devengado / pagado / cuenta por pagar | 10 500,00 / 8 664,00 / 1 836,00 |
| Composición: ingreso_flete / devolución / comisión / IVA flete / IVA devolución / IVA comisión / ajuste | 8 000,00 / 2 500,00 / 978,47 / 1 040,00 / 325,00 / 127,21 / 51 000,25 |
| Composición egresos: pago mensajero / ajuste / reverso cobro / otros / total | 9 500,00 / 500,10 / 0,00 / 0,00 / 153 845,77 |
| Desglose egresos: fijo / variable / sueldo / indemnización / total | 80 000,00 / 12 345,67 / 45 000,00 / 6 500,00 / 143 845,67 |
| Filas por libro (escenario) | caja 22 (sin el cobro) + 1 (`cobro_tienda`) = 23 · tienda A 9 · tienda B 9 · mensajero 7 |

Invariante (`caja-invariante-tiendas.test.ts`, 11 pasos): filas nuevas en caja por paso
`[23, 1, 1, 1, 1, 1, 0, 1, 1, 1, 1]`; R7 y R8 al céntimo tras cada paso salvo la foto legada (rota a
propósito por 1 000,00 y luego por 300,00).

## T0.2 — Mutaciones de control (a)–(e) de `design.md` §13, con autocomprobación

Protocolo por mutación (script `mutaciones-control.sh`, ejecutado desde el worktree): `sed` de UNA
línea → `git diff --numstat` debe ser exactamente `1 1` → `vitest run` SOLO sobre los archivos
nombrados → se anotan los `FAIL` con nombre de caso y la línea `Tests` → `git checkout -- <archivo>`
→ `git diff --stat` vacío. Las cinco cumplieron `1 1` y dejaron el árbol limpio.

| Mut. | Archivo · línea mutada | Tests corridos | Resultado |
| --- | --- | --- | --- |
| (a) | `LiquidacionService.ts` — `await this.caja.emitirEgresoDePago(tx, {` → `await Promise.resolve({` | fotografía + invariante | **ROJO** · `Tests 8 failed \| 18 passed (26)` · casos: «R93: Salió», «R95/R96: filas de la CAJA», «Entró, cifra principal, De las tiendas…», «R1/R2/R3 (461)», «R4 (461)», «dos siembras…», invariante «R8 (HD2)» y «anti-vacuidad» |
| (b) | `caja-tesoreria.ts` — `LIQUIDEZ.ingreso_flete: "cargo_a_tienda"` → `"efectivo"` | fotografía + invariante | **ROJO** · `Tests 3 failed \| 23 passed (26)` · casos: «Entró, cifra principal…», «R4 (461)», invariante «R8 (HD2)» |
| (c) | `desglose-tienda.ts` — `CUBETA.cobro_manual: "cargos"` → `"aFavor"` | fotografía + invariante | **ROJO** · `Tests 1 failed \| 25 passed (26)` · caso: «R92: saldo y desglose de la tienda B» |
| (d) | `CobroTiendaService.ts` — `await this.caja.emitirCargoDeCobro(tx, {` → `await Promise.resolve({` | fotografía + invariante | **ROJO** · `Tests 8 failed \| 18 passed (26)` · casos: «R1/R2/R3», «R27», «la ganancia sube EXACTAMENTE el cobro», «Entró, cifra principal…», «dos siembras…», invariante «R23–R26», «R8 (HD2)», «anti-vacuidad» |
| (e) | `PagoPorCuentaTiendaService.ts:179` — `await this.candado.bloquearBeneficiario(tx, {…})` → `void input.tiendaId;` | fotografía + invariante + `pago-por-cuenta-tienda-concurrencia` | **SUPERVIVIENTE** · `Tests 29 passed (29)` |

**(e) explicada, no escondida.** Es el mutante que la 459 ya midió como EQUIVALENTE hoy
(`pago-por-cuenta-tienda-concurrencia.test.ts`, cabecera): el pago de un gasto escribe ANTES de leer
nada y sus `INSERT` con FK a `usuario` toman `FOR KEY SHARE` sobre la fila de la tienda, que choca con
el `FOR UPDATE` del otro lado; su candado es defensa en profundidad. `design.md` §13 lo preveía: «si
ninguno la mata, la concurrencia de esta ficha la cubre». En la 457 el candado SÍ decide: el abono LEE
el saldo bajo el candado antes de escribir (R14/R15/R16), así que quitarlo (mutación 5 de la ficha) lo
tiene que poner rojo `abono-tienda-457-concurrencia.test.ts` (abono ∥ abono sobre la misma deuda).
Se mide en T9.1.

Logs: `mut_a.log` … `mut_e.log` en el scratchpad de la sesión; resumen copiado arriba.

## T0.4 — M3 y M4 medidos en el clon LOCAL (ANTES de escribir la migración)

`base: ordenex_457 @ localhost` (script de solo lectura con las consultas literales de `design.md` §14).

| Catálogo | n | Últimos |
| --- | --- | --- |
| `wallet_movimiento_categoria` | **23** | …, `ingreso_aporte_capital`, `egreso_reverso_aporte_capital`, `ingreso_cobro_tienda`, `egreso_reverso_cobro_tienda` |
| `wallet_tienda_movimiento_categoria` | **14** | …, `cobro_manual`, `pago_por_cuenta`, `pago_por_cuenta_anulado`, `cobro_tienda_anulado` |
| `wallet_origen_tipo` | **13** | …, `pago_por_cuenta_tienda`, `aporte_capital`, `cobro_manual_reclasificado`, `cobro_tienda`, `cobro_tienda_completado` |
| `historial_accion_tipo` | **61** | …, `aporte_capital_anulado`, `cobro_tienda_anulado`, `wallet_movimiento_manual_anulado` |
| `historial_accion_entidad` | **23** | …, `wallet_tienda_movimiento`, `pago_por_cuenta_tienda`, `aporte_capital` |

**Coinciden exactamente con `design.md` §1.1 (23 / 14 / 13 / 61 / 23) y con las listas de la 461.**

M4 (los dos CHECK, leídos de `pg_constraint`): caja = 12 ingresos (`ingreso_flete`, `ingreso_flete_devolucion`,
`ingreso_comision_cod`, `ingreso_iva_flete`, `ingreso_iva_flete_devolucion`, `ingreso_iva_comision_cod`,
`ingreso_ajuste`, `ingreso_cod_recaudado`, `ingreso_reverso_pago_tienda`, `ingreso_reverso_pago_por_cuenta_tienda`,
`ingreso_aporte_capital`, `ingreso_cobro_tienda`) + 11 egresos (`egreso_pago_tienda`, `egreso_pago_mensajero`,
`egreso_gasto`, `egreso_sueldo`, `egreso_ajuste`, `egreso_gasto_fijo`, `egreso_gasto_variable`,
`egreso_indemnizacion`, `egreso_pago_por_cuenta_tienda`, `egreso_reverso_aporte_capital`,
`egreso_reverso_cobro_tienda`); tienda = 4 créditos (`cod_recaudado`, `ajuste_credito`, `pago_por_cuenta_anulado`,
`cobro_tienda_anulado`) + 10 débitos (`flete`, `flete_devolucion`, `comision_cod`, `iva_flete`,
`iva_flete_devolucion`, `iva_comision_cod`, `pago_tienda`, `ajuste_debito`, `cobro_manual`, `pago_por_cuenta`).
**= las listas de partida de `design.md` §3.3.** Las migraciones de la Fase 1 parten de ESTAS.

M7: `to_regclass('public.abono_tienda')` = `null` (no existe); `liquidacion_pago` con `tienda_id` = 0.

T0.3 (producción, MCP de Supabase) la corre el LEADER: `progress/contraste_457.md`.

## T9.1 — anexo (se completa al cerrar la ficha)

Pendiente: repetir la fase 0 sobre el árbol final con las 14 mutaciones de `design.md` §13.
